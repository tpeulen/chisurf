
# Allow modules but ignore specific build directories
# Build artifacts
# Generated entry points (rattler-build)
# Ignore files in broken folder
# Local testing artifacts
# Test data -> TODO combine with tttr-data git
# Tests
# cmake
# cython
# macOS app building
# of pyx
# python
# pyx files have a tailing underscore ignore cpp files
# setup.py
# the api .rst files in api are generated by sphinx-apidoc and can thus be ignored
########################
###########################
#/test/broken/
#/test/data/
#Programming documentation
#docs
#mypy caches
#plugins
junk
*.cpython-*.dll
*.cpython-*.so
*.egg-info
*.exe
# Don't ignore JSON wholesale -- most JSON here is source (*.view.json view
# specs, manifest.json, registries, templates). Only ignore generated/cache
# JSON explicitly.
chisurf/core/mmfdb/data/_dictionary_cache.json
# Build-time generated static version (see setup.py / chisurf/core/info.py)
chisurf/core/_version.py
*.log
*.pyc
*.pyd
*.pyo
*_.cpp 
*scratch
chimol_ray_*.png
.*mypy*
.DS_Store
Thumbs.db
~$*
.coverage*
.idea
.ruff_cache
.omx
.kilocode
.opencode
.benchmarks
.ipynb_checkpoints
.mypy*
.pixi
.pytest_cache
.vscode
/htmlcov
/AGENT
/llm-wiki
/thirdparty
/scratch*
/build_tools/osx/resources/
/modules/*/.git/
/modules/*/b2/
/modules/*/build/
/test_*
_PROG
__pycache__/
_dictionary_cache.json
.env
act-output*.txt
broken
broken*
build/
build_tools/win/VC++*
chisurf/plugins/_dev*
cmake-build-*/
conda-bld/
dist/
doc/_build/
doc/api/
docs/_build/
docs/api/
output/
playground/*
rattler-build-log.txt
rattler-recipe/entry_points.json
# local scratch / junk
*.bak
session-*.md
dummy.db
wrapper_test.spc
scripts/*.txt
chisurf/plugins/core/acq/tcspc_devices/simulation/*.so
chisurf/plugins/core/acq/tcspc_devices/simulation/*.dll
# agent / AI assistant artifacts (kept local-only, never tracked)
.claude/
/CLAUDE.md
/AGENTS.md
/AGENT/
# internal dev notes / scratch (kept local-only). Drop loose notes & throwaway
# scripts in notes/ or scratch/ instead of the repo root so they stay ignored
# without adding a line here per file.
/notes/
# stray generated test images
/irf_*.png
/modules/imp-tricks

# Fluorophore staging database (relocated from _dev/fluorophore_db) — operational data, not tracked
chisurf/plugins/spectra_downloader/spectra.db
chisurf/plugins/spectra_downloader/spectra.db-shm
chisurf/plugins/spectra_downloader/spectra.db-wal
chisurf/plugins/spectra_downloader/spectra.db*.bak
chisurf/plugins/spectra_downloader/backups/

# Built by chimol's browser loader (`web/serve.py`); assembled on demand.
chisurf/plugins/chimol/chimol/web/chimol.zip
chisurf/plugins/chimol/chimol/web/pyodide/

# chimol's widget gallery: regenerable sheets from test/widget_gallery.py.
# Unlike test/renders/chrome_baseline/, these are NOT a baseline -- no test
# reads them, they exist to be looked at -- so they are output, not source.
chisurf/plugins/chimol/test/renders/widget_gallery/
