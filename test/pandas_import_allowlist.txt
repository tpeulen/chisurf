# Files in the shipped package that still import pandas.
#
# This is a SHRINKING list and a tracker: 47 non-test files when it was
# written, 11 now. It is not somewhere to add yourself. pandas is an interop format here
# -- a frame handed to a notebook, or a file only it can read -- not the storage
# model; the storage model is the columnar store behind chisurf.core.datastore.
#
# The list is in TWO groups, because a bare count overstates what is left.
# INTEROP entries are the ones pandas is kept FOR and are not expected to go;
# PORTS are the work. Moving a file between the groups is a decision worth
# stating in the commit.
#
# Enforced by test/test_pandas_seam.py, which fails on a new importer AND on a
# stale entry, so the count keeps meaning something.
#
# Removing pandas from the CODE does not remove the package from a solved
# environment: the conda pdb2pqr requires pandas >=1.0 outright, as do
# seaborn-base and statsmodels. What it wins is that ChiSurf's own tables stop
# being frames -- half the memory, dtypes that survive a file, and a missing
# value that does not have to be a NaN.

# ---- INTEROP: hands a frame OUT, or reads a format only pandas knows --------
chisurf/core/datastore.py
chisurf/core/structure/topology.py
chisurf/gui/plots/table_plot.py
chisurf/gui/widgets/chitable/filters.py
chisurf/gui/widgets/chitable/model.py
chisurf/gui/widgets/chitable/source.py
chisurf/plugins/burst/burst_analysis/api/workflow.py
chisurf/plugins/modelling/fret/evaluators/base.py

# ---- PORTS: three left, and none of them is mechanical ----------------------
# burst_fcs_correlator/wizard.py -- a wide pivot (groupby on two keys, set_index,
#   reindex) feeding an UNTESTED td4 writer that also violates the companion
#   contract; see okf/references/known-issues.md. Write the layout test first.
# burst_mle_analysis/wizard.py -- carries its OWN third copy of
#   read_burst_analysis. The fix is to delete it and use the shared reader, not
#   to port it.
# bid_to_analysis/__init__.py -- reads a .bur back to append to it; needs the
#   store CSV reader, which is deliberately not migrated yet (see PRD-82).
chisurf/plugins/burst/bid_to_analysis/__init__.py
chisurf/plugins/burst/burst_fcs_correlator/wizard.py
chisurf/plugins/burst/burst_mle_analysis/wizard.py
