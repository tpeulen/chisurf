---
type: PRD
prd: "131"
title: "PRD-131: fit_many everywhere — the per-row fit() loops in imaging and MFD"
description: img_pixel_mle, region_mle, mfd/fit.py's per-cell loops and pixel_maps.py's per-window CLSMImage constructions move onto fit2x's GIL-released batch entry; the process pool stays as mitigation until the batch is measured faster.
status: in-progress
created: 2026-09-02
updated: 2026-09-03
owner: tpeulen
sibling: prd-105.md
---

# fit_many everywhere

# Where to pick this up (2026-09-03, after review)

`aabb64e27` landed `Fit2xBatch` with a genuinely strict row-identical
parity test (8 cases including failure sentinels) and fixed a real defect
(`region_mle` hard-coded `r_scatter`/`r_experimental` to NaN on the batch
path). But only 1 of the job's 4 paths moved. Open, in order:

1. `region_mle` still falls to the per-row loop when `keep_curves` is
   true (the batch kernel returns no per-row model histograms).
2. `img_pixel_mle`'s threaded path still discards the harvested derived
   columns (`parallel.py`) — per-pixel `r_scatter`/`r_experimental`
   remain unavailable.
3. `mfd/fit.py` per-cell loops: untouched.
4. `pixel_maps.py` CLSMImage hoist: tried, reverted at 1.05x (recorded
   only in the commit body until now — this note is the record). The LRU
   cache is the standing mitigation.
5. Timing per path + the pool-removal decision: only the 1.05x number
   exists.


# Why

Phase-5 row: fit2x ships `fit_many` (GIL-released batch over rows), and
the imaging/MFD paths still call per-row `fit()` from Python loops —
`img_pixel_mle`, `region_mle`, `mfd/fit.py` per-cell/per-span,
`imaging/pixel_maps.py` constructing a `CLSMImage` per window. The
process pool currently papers over the loop cost; batching removes it.

# The job

1. Re-point `img_pixel_mle` and `region_mle` at `fit_many` (they already
   "reach for it" per the survey — finish the reach); results must be
   row-for-row identical to the per-row path, including failed-fit
   sentinel rows.
2. `mfd/fit.py` per-cell loops: batch the cells that share a model; the
   per-span structure decides the batch boundaries.
3. `pixel_maps.py`: hoist the `CLSMImage` construction out of the window
   loop (one image, many windows), then batch the per-window compute.
4. Measure each path before/after at a realistic image size; then decide
   whether the process pool is still worth its complexity on that path —
   deleting it is in scope where the batch alone wins.

# Definition of done

- [ ] No per-row `fit()` loop remains where `fit_many` applies; per-path
      row-identical parity pinned (sentinels included).
- [ ] One `CLSMImage` per image on the pixel-maps path.
- [ ] Timing per path; pool-removal decision recorded per path with its
      number.
- [ ] Phase-5 rows struck in [PRD-105](prd-105.md).
