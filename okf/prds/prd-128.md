---
type: PRD
prd: "128"
title: "PRD-128: MFD spectrum algebra onto the graph's own producers"
description: mfd/patterns.py's lifetime spectra, Perrin and polarised patterns re-implement what FretSpectrum/AnisotropySpectrum already compute for the TCSPC graph; the MFD side reads the same producers instead.
status: Planned
created: 2026-09-02
updated: 2026-09-02
owner: tpeulen
sibling: prd-105.md
---

# MFD spectrum algebra onto the graph's own producers

# Why

Phase-4 register row. `mfd/patterns.py` builds lifetime spectra, Perrin
anisotropy and polarised decay patterns in numpy — the same photophysics
`IMP.bff` `FretSpectrum`/`AnisotropySpectrum` compute as the TCSPC
graph's producers (the product of two sums of exponentials is a sum of
exponentials; a VV/VH pattern is a spectrum transform). Two owners of the
same photophysics is the `MaxEntLifetimeModel` failure shape.

# The job

1. Map each pattern function onto the producer-node algebra; anything
   genuinely missing (e.g. a pattern-specific normalisation) goes into
   the bff node, not beside it.
2. Parity A/B per pattern — VV *and* VH, with rotation terms armed;
   magic-angle-only fixtures prove half of a polarised setup (the census
   lesson).
3. Forward, delete the numpy bodies; the MFD callers keep their surfaces.

# Definition of done

- [ ] `mfd/patterns.py` computes no spectrum algebra of its own.
- [ ] VV/VH parity pinned with rotations armed.
- [ ] Register row struck in [PRD-105](prd-105.md).

# Traps

- ChiSurf normalises the *unpolarised, unquenched* amplitudes
  (`Lifetimes.amplitudes`); normalising downstream divides by `1 + 2 r0`
  after a rotation — the exact trap the decay builder documents. Reuse
  its settings (`set_absolute_amplitudes(False)`,
  `set_normalize_amplitudes(False)`), do not rediscover them.
