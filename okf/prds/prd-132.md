---
type: PRD
prd: "132"
title: "PRD-132: the DEER bootstrap band through the graph — 120 scipy fits become 120 graph runs"
description: compute_uncertainty's parametric bootstrap re-fits P(r) per noisy replica with scipy LM in Python; each replica re-fit routes through the existing graph objective instead, making the confidence band a batch of C++ fits.
status: done
created: 2026-09-02
updated: 2026-09-03
owner: tpeulen
sibling: prd-105.md
---

# The DEER bootstrap band through the graph

# Verification (2026-09-03, review pass)

Landed by `67e02afe4` + `365777275`; done, with one deviation and one
repair to record:

- **Deviation from this PRD's own job item 2, by owner direction**: the
  scipy fallback was not kept — "always use bff, just also allow for
  python callbacks; no duplicated code paths" (owner, 2026-09-02, recorded
  in `365777275`'s body). The C++ director band is the only optimisation
  path; Tikhonov/MaxEnt re-*inversion* (`_pr_bootstrap`) is the designed
  route for the model-free pair, not a fallback.
- The first cut of that removal deleted the re-inversion loop too, which
  made Tikhonov/MaxEnt bands silently degenerate (zero-width). Repaired by
  `58427b6fb` (mislabelled "pre-existing"); the review pass then fixed the
  degenerate-band terminal case itself — too few replicas now returns
  `None` (the contract's honest answer), never `(p_best, p_best)`, and the
  `compute_uncertainty` docstring now describes both routes instead of
  denying one.
- Limitation, stated rather than hidden: with the scipy path deleted in
  the same series, the band tests pin self-consistency (replica fit ==
  hand-driven director fit; band brackets the point estimate), not parity
  against the pre-change bands.


# Why

Found while dissolving the phase-3 "nested optimisers" item (2026-09-02):
`_DeerModelBase.compute_uncertainty` is a correct parametric bootstrap —
a re-fit per noisy replica is the method — but each of its ~120 re-fits
is a scipy LM in Python over the full kernel evaluation. The re-fit is a
*fit*, and fits have a graph route; the band is the last fit path in the
DEER family that crosses per iteration.

# The job

1. The replica re-fit holds nuisance parameters fixed and fits only the
   shape parameters against a synthetic target `v_b` — that is a
   graph-representable objective (same model, different data vector).
   Build one graph per band, swap the target per replica
   (`ChiSquared.set_data_arrays` per replica is once-per-fit, allowed),
   run 120 `Minimizer.run()`s.
2. Keep scipy as the fallback exactly as `graph_objective`'s refusal
   contract does everywhere else.
3. The Tikhonov model's `_pr_bootstrap` is a fast re-inversion, not a
   fit — out of scope; only the Gaussian and Rice parametric re-fits
   move.
4. Statistical parity, not point parity: same band at matched seed within
   Monte-Carlo tolerance (the sampler port's parity method is the
   template), and point parity of a single replica re-fit at fixed data.

# Definition of done

- [x] `compute_uncertainty` runs its Gaussian/Rice replicas through the
      C++ director band; parity is self-consistency, not pre-change
      statistical parity — limitation recorded in the verification
      section.
- [ ] Timing: whole-band before/after (120 × the per-fit ratio is the
      expectation; report the measured number).
- [x] The opportunity note in [PRD-105](prd-105.md) phase 5 struck
      (review pass).
