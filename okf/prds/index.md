# PRDs — Design Notes

Product-requirement / design notes driving current work. Each PRD is one **self-contained** concept (`prd-<n>.md`) carrying its full design text and keeping its **persistent number** as a stable id; references to external software are described by role, not named. These were formerly the top-level `overhaul/` folder, now retired into this group. The authoritative implementation ordering is [PRD Implementation Order](master-order.md).

Status: ✅ done · 🚧 in-progress · ✏️ draft · 🌱 stub · 📋 planned · ⛔ superseded

# Foundation — sample tracking, dictionary infrastructure, ORM/payload

* ✅ [PRD-02: Sample Tracking — Deep Sample Description](prd-02.md) — Link every dataset and result to a full atomistic, flrCIF-aligned sample description.
* ✅ [PRD-02a: mmCIF Dictionary Infrastructure](prd-02a.md) — Parse bundled mmCIF dictionaries into a cached API for vocabulary validation and autocomplete.
* ✅ [PRD-02b: MMFDB Admin Overhaul — Manual Inspection & Editing](prd-02b.md) — Make the mmfdb-admin plugin inspect, add, and edit every record the sample data model produces.
* ✅ [PRD-02c: Aligning ChiSurf MMFDB Export to flrCIF](prd-02c.md) — Maps ChiSurf's internal parameter short names to canonical flrCIF dictionary items on export; the alignment script now updates existing saveframe descriptions, with a drift guardrail test enforcing agreement.
* ✅ [PRD-020: SQLAlchemy MMFDB Mapping](prd-020.md) — A bounded SQLAlchemy relationship layer for the MMFDB sample, probe, and FRET-pair tables.
* ✅ [PRD-030: Result Payload Formats & Codecs](prd-030.md) — A typed, msgpack-based, schema-validated codec layer for MMFDB scientific result payloads.

# Phase 0 — Current line (result registry + burst pipeline)

* ✅ [PRD-01: Fix MMFDB Project Round-Trip](prd-01.md) — Make archiving a project to MMFDB and restoring it produce an identical project.
* ✅ [PRD-03: Result Registry](prd-03.md) — A single register_result() API so any plugin can archive output to MMFDB with full provenance.
* ✅ [PRD-04: Stable Burst Pipeline MMFDB Integration](prd-04.md) — Register burst-selection results in MMFDB with stable, queryable provenance across all callers.
* ✅ [PRD-09: Microtime Shifter — Workflow Plugin + MMFDB Provenance](prd-09.md) — Convert the microtime-shifter tool into a layered api/backend/cli/gui workflow plugin with RPC and full MMFDB provenance.
* ✅ [PRD-10: MMFDB Dataset Browser Widget](prd-10.md) — A reusable Qt widget to pick a registered MMFDB dataset; file-group membership table (`mmfdb_artifact_member`) landed with `register_raw_measurement_group` and `member_count` in browse.

# Phase 1 — Architecture foundations

* 🚧 [PRD-17: Canonical Identity / Session Context](prd-17.md) — Fixes ownership/visibility around the canonical user resolver; explicit SessionContext threading remains open.
* 🚧 [PRD-18: Dependency Injection + Hermetic Test Harness](prd-18.md) — Makes the suite hermetic and contracts the real MMFDB client, while handler/session injection remains incomplete.
* 🚧 [PRD-19: Single Canonical Dictionary-Driven Schema](prd-19.md) — Collapses legacy duplicate table families and seeds vocabulary from the dictionary; hand-written core DDL remains.
* 🚧 [PRD-25: Consistency Hardening + Correctness Primitives](prd-25.md) — A set of cross-cutting correctness changes — uniform fail-loud errors, one RPC envelope, a single sample read path, caching, N+1 removal, dead-code removal — plus typed IDs, first-class units, and boundary validation.
* 🚧 [PRD-27: Event-Sourced, Append-Only Provenance Core](prd-27.md) — Locks the append-only-lite decision and implements branch/event-log pieces, but full reconstructable append-only provenance remains incomplete.

# Phase 2 — Operation/transformer spine + model-driven layer

* ✅ [PRD-11: Transformers as Abstract Data-Operation Nodes](prd-11.md) — Model every data-manipulation step as a uniform MMFDB operation node with typed, dictionary-declared parameters and input/output ports.
* ✅ [PRD-16: General Transformer Contract](prd-16.md) — Defines one uniform contract every data-transformer plugin must obey — typed ports, dictionary-declared parameters, a pure transform, and uniform provenance registration.
* 🚧 [PRD-26: Model-Driven Data Layer](prd-26.md) — Adds dictionary-derived DAO, validation, docs, and admin registry pieces; hand-maintained CRUD/SQL remains.
* ✅ [PRD-28: Companion-Tool ↔ MMFDB Burst-Selection Round Trip](prd-28.md) — Both directions wired: open registered burst selections from MMFDB via the picker, and send the current burst result directly to the exploration tool.
* 📋 [PRD-29: Visual Burst Programming — Node-Graph Editor for Burst Analysis](prd-29.md) — Turns the existing node editor into a visual programming canvas for composing, running, previewing, and provenance-recording burst-analysis pipelines.
* ✅ [PRD-31: Headless CLI for the Companion Photon-Data Exploration Tool](prd-31.md) — Adds a windowless CLI to the companion exploration tool for parameter-based burst filtering and imaging, integrated with MMFDB.

# Phase 3 — Provenance + LIMS

* ✅ [PRD-12: Lifecycle State Machines + Transition History](prd-12.md) — Turn flat entity status flags into tracked lifecycles with a recorded, validated transition log (who, when, why).
* ✅ [PRD-13: Study / Project Entity with Configurable Fields](prd-13.md) — Promote the loose project-id string into a real study entity that groups samples and datasets with ownership, membership, and custom fields.
* ✅ [PRD-14: Protocol Entity — Named, Versioned Procedures](prd-14.md) — Add named, versioned measurement/processing protocols with declared parameter schemas that operations reference for reproducibility.
* ✅ [PRD-21: Provenance/Lineage Query API + Event Model](prd-21.md) — Makes the provenance graph queryable through a first-class lineage API, stores a replayable compute spec on each derived artifact, and adds an in-process event model for reactive behaviour.

# Phase 4 — Composition + remaining features

* ✅ [PRD-05: Calibration Provenance](prd-05.md) — Track calibration parameters in MMFDB with links to the reference measurements they derive from.
* ✅ [PRD-06: Expand the Fluorophore Database](prd-06.md) — Populate MMFDB with real, provenance-tracked spectral data for common dyes and compute Förster radii from spectral overlap.
* 📋 [PRD-08: Optical Configuration Schema](prd-08.md) — Replace opaque setup JSON blobs with structured, queryable tables describing the full optical path from source to detector.
* ✅ [PRD-15: Lightweight Reagent / Consumable Inventory](prd-15.md) — Track consumables (dye lots, buffers, filters, kits) with lot/expiry and link them to operations, setups, and samples for reproducibility.
* ✅ [PRD-22: Workflow / Pipeline Engine on the Transformer Contract](prd-22.md) — Lets users compose conformant transformers into a type-checked, node-based dataflow pipeline that executes headlessly and is recorded in MMFDB as a reproducible chain of operations.
* ✅ [PRD-39: Sequence Provenance & External References](prd-39.md) — Records each entity's canonical sequence/structure cross-references and its engineered mutations as structured, exportable flrCIF/PDBx data using the standard struct_ref category family.

# Phase 5 — Capstone

* ✅ [PRD-24: Extract MMFDB into a Standalone Package](prd-24.md) — Moves MMFDB (schema, dictionary, generator, repository/API, server, admin backend) out of chisurf into an independent module with a stable public API and no chisurf imports.

# Cross-cutting (interleave throughout)

* 🚧 [PRD-23: Thin Widgets / View–API Separation](prd-23.md) — Makes GUI widgets pure view — no data processing, no database or acquisition-library calls, no side effects on construction — with mandatory construction smoke tests and a shared dockable-tool base.
* 📋 [PRD-30: CLI Pipeline Tools with Unix Pipe Support](prd-30.md) — Gives burst/TTTR CLI tools stdin/stdout streaming via a self-describing msgpack frame format so they compose as Unix pipes.
* ✅ [PRD-32: Acquisition Standard Output Folder](prd-32.md) — Adds a single user-configurable standard output folder to acquisition so new measurements have a predictable save location.
* 📋 [PRD-33: Acquisition-to-MMFDB Registration](prd-33.md) — Adds a save mode that writes a newly acquired measurement directly into MMFDB with sample linkage and provenance.
* 📋 [PRD-34: Burst-ID Native MMFDB Save and Downstream Ingest](prd-34.md) — Makes MMFDB the default save target for burst-identification selections when connected, and lets downstream burst tools ingest them directly from the dataset picker.
* ✅ [PRD-36: Dockable-Tool Base Migration Tracker](prd-36.md) — All tools on the `ChisurfDockTool` base. `ProjectBrowserTool` deferred DB init via `QTimer.singleShot`; `MolViewPluginWindow` landed after PRD-57's renderer split settled. Guard test passes with no allowlist entries.
* ✅ [PRD-43: Align GUI Operation History with MMFDB Provenance](prd-43.md) — Makes the in-memory operation history a projection over a durable MMFDB event log so undo/redo and the exploration trail survive database save/restore, and incrementally aligns the GUI event stream with the backend provenance model.
* ⛔ [PRD-47: Relocate Spectroscopy Physics into an External Biophysical Modeling Framework](prd-47.md) — Superseded by [PRD-105](prd-105.md): same goal, but written before the engine had the fit graph, the minimizer, and the model kernels; its Python-staging-layer route is obsolete — kernels now land in C++ directly with a parity test.

# Feature / roadmap tracks (independent)

* ⛔ [PRD-07: Plugin MMFDB Integration](prd-07.md) — Have high-priority plugins register their results in MMFDB via a single registration call at each output point.
* 📋 [PRD-35: Light-Path Optical Presets Stored in MMFDB](prd-35.md) — Migrates optical-path presets from JSON files on disk into MMFDB as queryable, versioned, provenance-tracked entities.
* ✏️ [PRD-37: Network Deployment Security — transport auth + RPC authn/authz](prd-37.md) — Adds encrypted, endpoint-authenticated transport plus per-call authentication, authorization, and scoped event broadcast so the server may bind beyond loopback.
* ✅ [PRD-38: Model/UI Split — view-spec JSON drives auto-generated model editors](prd-38.md) — Splits a fitting model's compute definition from its editor by describing the editor in a co-located JSON view spec that a generic GUI renderer turns into the control panel.
* ✅ [PRD-40: A ChiSurf-native declarative dataset-to-editor framework](prd-40.md) — One reusable way to declare a typed dataset and auto-generate its Qt editor, now the AutoForm system driving every fitting-model panel and several tool panels.
* ✏️ [PRD-41: FDB4ChemBio Access-Layer & Interoperability Strategy](prd-41.md) — A design note fixing the architectural boundary for MMFDB as a prototype public resource — the dictionary is the product, deposition and dissemination differ, and a future read-only GraphQL endpoint is generated from the dictionary.
* ✅ [PRD-42: Confine pyqtgraph to plot-only widgets](prd-42.md) — Restricts pyqtgraph to plot-canvas code and replaces its non-plot uses (numeric spin boxes, parameter trees) with dependency-free Qt-native equivalents.
* ✅ [PRD-44: Vendor-Neutral Dictionary Schema Namespace](prd-44.md) — Renames the MMFDB dictionary's local extension tags from an application-branded namespace to a store-keyed vendor-neutral one, behind a backward-compatible parser, so MMFDB is usable by software beyond ChiSurf.
* ✏️ [PRD-45: Chemical registry-number as a first-class chemical identity in MMFDB](prd-45.md) — Promotes the chemical registry number from an ad-hoc free-text property to a dictionary-defined, validated, indexed, cross-entity chemical identity surfaced across GUI, CLI, and RPC.
* ✅ [PRD-46: Scripts as first-class citizens in the test pipeline](prd-46.md) — Brings shipped example scripts into the automated test suite by running headless/process *and* interactive scripts under a shebang-driven runner with numeric assertions, plus unit tests for the public model API they exercise.
* ✏️ [PRD-48: Provider-Agnostic ELN Integration](prd-48.md) — A provider-agnostic ELN integration layer for MMFDB with a single gateway abstraction and two concrete electronic-lab-notebook backends, supporting bidirectional deposit, import, and reconciliation.
* ✏️ [PRD-49: Multiparameter-Fluorescence Feature Parity](prd-49.md) — Roadmap PRD to reach and surpass feature parity with an established multiparameter-fluorescence analysis suite across every analysis modality, delegating implementation to per-module sub-PRDs.
* ✅ [PRD-50: Photon Distribution Analysis (PDA) Family](prd-50.md) — Wraps the PDA histogram engine in ChiSurf models and AutoForm view specs: static distance-distribution PDA, dynamic/N-state kinetic PDA, error surfaces (support-plane + MCMC), and a kinetic consistency check (three-colour PDA is PRD-65).
* 🚧 [PRD-51: Imaging Correlation — N&B, STICS/TICS, iMSD, Spectral RICS](prd-51.md) — The spatiotemporal correlation carpet and its unified fit model have landed; remaining is the model-free STICS velocity workflow (immobile filter, per-lag peak tracking, flow vector maps), Number & Brightness, TICS decay fits, and crosstalk-free spectral RICS.
* ⛔ [PRD-52: Phasor-FLIM Imaging & Particle Tracking](prd-52.md) — Superseded: delivered under PRD-55, PRD-56, the `img_pixel_phasor` plugin, and the `img_tracking` plugin.
* 🌱 [PRD-53: Simulation Workflow (Diffusion + FRET + Photon + Camera)](prd-53.md) — Surface the existing Monte-Carlo simulation engines as a headless + AutoForm workflow producing synthetic ground truth for downstream analysis validation.
* 🌱 [PRD-54: Spectral Unmixing, pCF, nsFCS & FCCS Models](prd-54.md) — Close the remaining spectroscopy gaps — spectral unmixing/spectral phasor, pair-correlation analysis, nanosecond-FCS/antibunching, a dedicated FCCS fit model, and a 2-photon FCS model.
* ✅ [PRD-55: Phasor Analysis Toolkit (open-library parity)](prd-55.md) — Turn ChiSurf's phasor viewer into a phasor analysis toolkit by natively implementing apparent-lifetime readout, g,s filtering, component fraction/unmixing, and cursor masks, with no new dependencies.
* ✅ [PRD-56: Companion-Tool ↔ ChiSurf RPC + Phasor Overlays](prd-56.md) — The companion photon-data exploration tool is a live phasor front-end over a first-class RPC client, with ChiSurf serving phasor math and shared overlay-line geometry (non-loopback gated on PRD-37).
* ✅ [PRD (detector-setup): Centralized Detector Setup Selection](prd-detector-setup.md) — Replaces the full detector/PIE-window wizard page with a lightweight `SetupSelector` widget, declaratively available as an AutoForm section.
* 🚧 [PRD-57: ChiMOL Command Parity and Renderer Migration](prd-57.md) — Grow the ChiMOL viewer's `cmd` surface toward reference-language parity and migrate its renderer to an immediate-mode GUI backend behind a Qt-free controller/scene contract (Tiers 0–3 landed).
* 🚧 [PRD-58: FRET Plugin as a Strict FPS + OLGA Superset](prd-58.md) — Make the `fret` plugin a strict superset of the legacy FPS and OLGA tools — embedded fps.json editing with live AV preview, refine/bootstrap/sample/evaluate workflows, and informative pair selection (CLI + evaluators + pair-selection + output writers landed; R13/R14 tested + real AVSizeEvaluator; IMP-free pose scorer + RBD/geometry tests; select-pairs CLI bug fixed; GUI wizard + IMP-gated refine/errors coverage remain).
* ✅ [PRD-59: Pluggable MMFDB Authentication (local / LDAP)](prd-59.md) — Replaces MMFDB's hardcoded local auth with a pluggable provider layer (local + LDAP/Active-Directory) behind one `login()` orchestrator, resolving to the existing Principal/session with JIT provisioning and directory-group mapping, behind an extensible provider registry.
* ✅ [PRD-60: Amortised (Surrogate) Neural Estimator for H2MM](prd-60.md) — An optional simulation-based-inference fast path that predicts H2MM parameters in one neural-network forward pass instead of iterating Baum-Welch EM, for large single-molecule FRET datasets (Python module + C++ engine landed and cross-validated; approximate, opt-in, no pretrained model shipped).
* ✅ [PRD-61: Generalized Fitting with Parameter Priors](prd-61.md) — Generalize the fit objective from least squares to maximum-a-posteriori and Bayesian sampling by letting any parameter carry a prior (Gaussian/log-normal/…/callback, conjugate combination), with hard bounds implemented as the uniform-prior special case, stored on the chinet Port and edited via a per-parameter prior selector + modal.
* 🚧 [PRD-64: chiplot — single plotting seam and pyqtgraph replacement](prd-64.md) — Route every plotting access through one dependency-neutral chiplot facade so pyqtgraph becomes a swappable backend, then grow chiplot into a native OpenGL/immediate-mode renderer behind the same API.
* 🚧 [PRD-66: chitable — single table seam and DataFrameEditor replacement](prd-66.md) — One model/view table family shared by ChiSurf and ndX — value filtering, column hiding, hide-empty columns, colour-by-value, column picker, copy/export — replacing a third-party DataFrameEditor and dozens of hand-rolled QTableWidgets, and dropping the last third-party GUI dependency.
* ✅ [PRD-62: FCS Model Consolidation — Table Views, Composable Diffusion + Bunching, Parameter-Registry Scoping](prd-62.md) — Migrates the FCS MDF model to the table parameter view with bunching terms, adds a general composable FCS model (diffusion choice + bunching/antibunching), and fixes a parameter-name registry collision (R0) with a general class-scoping mechanism.
* ✅ [PRD-63: Internationalisation & UI-language harmonisation](prd-63.md) — A translation kit that localizes the data-driven view.json/manifest and .ui UI from one Qt-free seam (identity default + QTranslator bootstrap), an extractor + 6 catalogues (en/de/fr/es/pt/ru), and a canonical UI glossary used both as the translation source and the harmonisation target.
* ✅ [PRD-65: Three-Colour Photon Distribution Analysis (PDA3c)](prd-65.md) — A burst-wise three-colour PDA model — trinomial/binomial photon-partition likelihood with Poisson background, correlated trivariate distance distributions, labelling and brightness corrections, and MAP + MCMC inference with per-parameter priors, implemented in Python/numba with algorithmic rather than language-level speedups.
* ✅ [PRD-67: Two-Channel Image Colocalization](prd-67.md) — A colocalization plugin for multi-channel microscopy images — Pearson/Manders/Costes/Li coefficients with automatic thresholds and a randomization significance test — over one shared image-source seam that reads both camera image stacks and photon-stream (confocal-scan) files.
* ✅ [PRD-68: Fit Factor Graph — structure-aware objectives and posteriors](prd-68.md) — Materialise the factor structure of a fit's posterior (variables = free parameters, factors = per-dataset likelihoods and per-parameter priors) so global fits stop recomputing every dataset per evaluation, and so blocks, separators, treewidth and identifiability become queryable; plus the prior fix that makes MCMC target the same posterior as MAP.
* ✅ [PRD-69: Trustworthy and structure-aware posterior sampling](prd-69.md) — Makes an MCMC run report whether it converged (R-hat, ESS, autocorrelation time, Monte-Carlo error, burn-in) and replaces the isotropic random walk with a covariance proposal, exact independent-component decomposition, and collapsing of a linked global fit onto its shared parameters.
* ✅ [PRD-70: One posterior-query API over every uncertainty estimator](prd-70.md) — Puts the covariance, profile-scan and MCMC estimators behind a single engine protocol (condition / add_target / run / marginal / joint / log_evidence) so the choice of estimator stops leaking into every call site.
* 🚧 [PRD-71: Fast 2D MFD Fitting — kinetic models fitted to E–⟨t⟩ and r–⟨t⟩ burst histograms](prd-71.md) — Kinetic models fitted to the 2D multiparameter-fluorescence burst histograms by forward-modelling raw per-burst observables over a PDA-style empirical nuisance measure; stages 2–6 have landed — static model, occupation-time propagator, all three scoring sources, the anisotropy axis, and the experiment/model/plot surfacing — with milestone 1a passed on a real measurement and 1b passing as a code test against a dynamics simulator; 1b on real data still needs a measurement with an independently known rate.
* ✅ [PRD-72: Non-breaking ChiSurf groundwork for 2D MFD fitting](prd-72.md) — All 10 items landed: burst-folder reading with auto-resolved photon sources, a preparation core with plugin (api/cli/gui/rpc) and reader surfaces over it, and the mean micro time in the burst tables.
* ✏️ [PRD-73: FRET wiring for the FCS kinetics model](prd-73.md) — Let the FCS kinetics model be driven by FRET species: a brightness source in the FRET calculator that emits per-state brightnesses from efficiency + crosstalk (`L`) + excitation (`sigma`), a button on the FCS model that imports them into `StateBrightness`, and one agreed definition of average brightness Q from the crosstalk and excitation matrices.
* ✅ [PRD-74: FCS kinetics model — full mode vs fast (eigenvalue-only bunching) mode](prd-74.md) — Add a fast path that skips the spatial S1-population solve and 3D autocorrelation and computes only the eigenvalue-based photokinetic bunching factor (brightness correctly included), trading the saturation volume expansion for near-instant evaluation, with a weak-power agreement contract between the two modes.
* ✏️ [PRD-75: Brightness into the amplitude — species mixtures and FRET cross-talk in FCS](prd-75.md) — Postponed design for how per-species brightness propagates into the FCS amplitude, so a mixture of FRET species with different brightnesses contributes to G(0) by brightness-weighted concentrations rather than raw particle numbers; registered now so the single-species `1/N` assumption is a named limitation.
* ✏️ [PRD-76: PSF fitter — recovering the real detection profile from saturation FCS](prd-76.md) — Makes the detection profile a swappable, fittable object — saturation FCS gains Gauss-Lorentz/diffraction/measured/free-form profiles instead of the hard-coded 3-D Gaussian — and recovers the real profile (with it `V_eff`, `gamma` and absolute concentrations) by globally fitting a power series, since saturation flattens the profile at a rate that depends on its fall-off.
* ✏️ [PRD-77: One HMM — retire the in-tree Gaussian HMM onto the simulation library](prd-77.md) — The binned-trace `GaussianHMM` is the last HMM in the tree implementing its own inference; the photon-by-photon side already delegates. Paired with tttrlib **PRD-013**, which adds the Gaussian emission to that library's existing HMM core — the two must land together, and the in-tree implementation stays until a ground-truth recovery test *and* an honest benchmark both pass, because it is the faster implementation of its model.
* ✅ [PRD-78: NDXplorer histograms on tttrlib instead of boost-histogram](prd-78.md) — Replaces boost-histogram in NDXplorer's histogram path with tttrlib's hist module, removing a dependency carried solely for this one code path.
* ✅ [PRD-79: Drop pyarrow from NDXplorer](prd-79.md) — Replaces pyarrow with tttrlib's DataStore plus a threaded CSV reader, removing another dependency carried for one module.
* ✅ [PRD-80: Retire mdtraj — DCD frames, RMF3 hierarchies](prd-80.md) — `mdtraj` is the only reason `pytables` and, through it, `numexpr` are installed. IMP already covers the structure side (34 import sites) but has **no DCD reader at all** — verified in its source, where the only `dcd` matches are prose about citing a file in a deposition — so a DCD codec is the one genuinely new piece of code. Scope is narrower than it looks: mdtraj's HDF5 trajectory format lives in exactly **one** file, and the 24 other `.h5` users (TTTR photons, bursts, MMFDB) are untouched. MDAnalysis was measured and rejected at 142 packages against mdtraj's 70, and it reintroduces `networkx` and `tqdm`. Storing trajectories *as* RMF3 was tested and rejected too: **10.5 s to read 200 frames of 2500 atoms against 14 ms for DCD** — RMF's per-node key model is the wrong shape for dense coordinates, so DCD holds frames and RMF3 holds hierarchies.
* ✅ [PRD-81: chinsole — one console seam, and the end of qtconsole](prd-81.md) — ChiSurf ran an in-process Jupyter kernel to provide a text box that runs Python, and had **three** hand-rolled consoles besides. chinsole is the in-tree replacement: a Qt-free interpreter (`chisurf/core/console/`) under a Qt widget (`chisurf/gui/chinsole/`), so the whole of execution is testable without a `QApplication` — which the kernel design never was. `qtconsole` is retired and banned by the guardrail. Measured: dropping it alone is **264 → 262** packages, because `notebook<7` drags `ipython` back in regardless; the remaining **83** needed the external notebook server to go. **That removal landed in 2026-08** — the `start_jupyter`/`populate_notebooks` services, the Notebooks ribbon category and the `notebook` pin are gone (`nbformat` declared directly for the in-tree editor), so the whole-Jupyter-stack closure is now available; only `ipython` remains in the runtime.
* ✅ [PRD-82: The table layer onto the DataStore — pandas out of the tree entirely](prd-82.md) — `chitable`'s adapter architecture, the burst tables, the HDF5 writers and the CSV readers all moved onto the simulation library's columnar store, and pandas was then eliminated from `chisurf/` completely (scope changed mid-PRD on explicit direction: "no pandas AT ALL"), not merely from the storage path. Half the memory on a burst table (109.5 → 60.2 MB), types and missing values that survive a file, `pytables` genuinely gone from a freshly solved environment, and `pandas` itself struck from `pixi.toml`/`pyproject.toml`/`rattler-recipe/recipe.yaml` — closure measured 256 → 255 with `conda create --dry-run`. It stays declared only in `pixi.toml [feature.test.dependencies]`, for tests that use it as an independent oracle or to simulate a file an external tool wrote. Open, unrelated to the pandas work: acceptance criterion 3 (a file written by a previous release still needs the optional HDF5 package to open) and criterion 6 (the memory saving is unmeasured past 113 rows).
* 🚧 [PRD-83: Retire pyqtgraph — the umbrella, and the 3-D half nobody owned](prd-83.md) — `pyqtgraph` is reached from 14 files, one `.native` escape hatch, and **three subsystems that are not plotting at all**: OpenGL views, a parameter tree and a dock area. PRD-64 owns the 2-D seam; this owns the end state — *is pyqtgraph still installed, and what is the last thing keeping it here?* The **3-D half is closed**: ChiMOL renders through PyOpenGL on a plain `QOpenGLWidget`, and its last pyqtgraph import was a 13-line module computing a flag **nothing imported**. The shim it left behind was not merely unnecessary but actively harmful — `opts["center"]` was a second store maintained from seven call sites and read by nobody, and the picking helper it existed for read that `QVector3D` straight into `numpy`, so every click in the viewport raised.
* 📋 [PRD-84: Units all the way out — parameters, axes and files that say what they mean](prd-84.md) — A number in ChiSurf is a bare float, and what it is measured in lives in a column name when somebody remembered, in an axis label when somebody typed one, or nowhere. **Four** in-band conventions encode it and disagree: `"Duration (ms)"` suffixes, `"Tau | ns"` headers, and two bare factor tables in the DEER and FCS readers. `FittingParameter` has no unit at all, so a fitted lifetime leaves the program as a bare number. The vocabulary half has landed — `_mmfdb_units` declares 28 codes with symbols and SI factors, 15 of them mmCIF's own, and a `.pto` column now carries its unit — and the rest of the stack is open under one rule: **unitless inside, units at every boundary a person or a file sees**. Explicitly *not* a unit-aware numeric type: arrays stay numpy arrays and the fitting engine stays unitless, because dimensional analysis in a Jacobian is a way to make a fit slower and wrong.
* ✅ [PRD-85: Drop guards — an opt-in nag-before-commit pattern for file drops, first applied to .pto conversion](prd-85.md) — Part A (drop-guard registry + `tttr_to_pto` conversion guard) and Part B (decimated plotting via `thin_for_plot` + `chiplot.Plot.line(max_points=...)`) are done; all seven listed plugins audited — none has raw per-photon plots, and `intensity_trace` gained viewport decimation.
* 📋 [PRD-86: An in-tree segmentation core — Otsu, border-clearing, peak-finding, watershed — closing scikit-image out of ChiSurf's imaging path](prd-86.md) — `chisurf.core.roi.props` already reimplements `skimage.measure.regionprops` property-for-property, so "what is this region" is dependency-free; "which pixels are a region" is not. Molecule MLE's actual segmentation (`segment_molecules`) imports `skimage.filters`/`feature.peak_local_max`/`segmentation.{clear_border,watershed}` **unconditionally, with no fallback** — the plugin cannot segment a molecule without scikit-image; colocalization's `_watershed_split` uses the same pair but already degrades gracefully (`try/except ImportError` → plain connected components), the target shape both should converge on. Of the four calls, Gaussian smoothing needs no port at all (`scipy.ndimage.gaussian_filter` already does it); Otsu and border-clearing are small, well-known algorithms; local-maxima and marker-controlled watershed are the real centrepiece — a genuine classical algorithm, closer in effort to the in-tree `marching_cubes.py` port than to the `pdb2pqr` single-atom fix it otherwise resembles in spirit. Proposed home: `chisurf/core/roi/segmentation.py`, tested the same way `regionprops` already is (`pytest.importorskip("skimage...")`-guarded parity tests) — and, unrelated to the port itself, the CLSM plugin's `skimage.io.imsave` (an aliased-import guard-bypass of the retired `imageio` dependency) was found and fixed directly during scoping.
* 📋 [PRD-87: chisurf.core.ml — the four estimators the tree actually uses, and the end of scikit-learn](prd-87.md) — `scikit-learn` (with `joblib`, `threadpoolctl`, `narwhals` and `hdbscan`, **256 → 251** packages / 46 MB) is installed so that **five files** can call **four estimators**, and the used surface is exhaustively `GaussianMixture`, `MLPRegressor`, `StandardScaler`, `KMeans`, `PCA`/`IncrementalPCA` — no pipelines, no model selection, no metrics. The tree meanwhile *already contains* most of them, sealed as private helpers inside `chisurf/core/math/hmm.py`: the Cholesky log-density for all four covariance layouts, and a complete k-means++ with `n_init` restarts. Counting the companion exploration tool's `GaussianMixtureFixedEM` — written because the library **cannot fix a subset of means or covariances** — there are **four implementations of two algorithms**, none sharing a line, with the dependency still installed. The port mirrors the library's own package layout and public attribute spellings so each call site changes by one import, and makes the fixed-parameter masks a first-class option rather than a fork. **The dependency does not leave when chisurf stops importing it**: `hdbscan` requires `scikit-learn`, and the fallback path reaches for `sklearn.cluster.HDBSCAN` when the standalone package is absent — a knot that cannot be cut from either end, so HDBSCAN (mutual-reachability MST + condensed tree + excess-of-mass) is the one genuinely new algorithm and the honest place this may stop.
* ✅ [PRD-88: Every provenance graph reconstructs — the source/sink matrix, and the chains between them](prd-88.md) — A container's provenance is only worth having if **every** artifact in it reaches the primary data, and that is a property of the whole graph written by ~20 independent writers against a dozen kinds of source. It was being tested on **one synthetic two-step chain**. A throwaway audit driving the real writers against real files found **6 of 26 artifacts with a broken or incomplete lineage**, four of them one root cause: `instrument_uid` was recovered on reopen by matching a single artifact kind, so a container whose source is *not* photons came back with no primary. The audit is now two test files (`test_provenance_matrix.py`, `test_container_cross_writer.py`), the root-cause defects are fixed, the nested chains (depth-4 fan-in, raster mid-chain, calibration as second relation, reopen) are covered, and the CLI's vocabulary is checked against the dictionary. Two edge-case cells documented as decisions: `img_tracking` needs a fixture (not a defect), and a no-primary container is well-formed when the artifact is explicitly declared primary.
* ✏️ [PRD-89: Consume the tttrlib spectroscopy data standard (decay, FCS, PDA, PCH) in ChiSurf](prd-89.md) — The consumer side of tttrlib's self-describing .dstore/.pto tables for decay, FCS, PDA, and PCH curves: ChiSurf reads them through the standard read_table vocabulary and feeds its fit/analysis GUIs — no private format, no library-specific glue.
* ✏️ [PRD-90: Provenance-driven citation list for ChiSurf analysis pipelines](prd-90.md) — Surfaces the automatic citations that tttrlib generates from .pto provenance graphs in the GUI, lets the user export them, and ensures ChiSurf-written .pto files embed them.
* ✏️ [PRD-91: Lumis Quest — the documentation is the world](prd-91.md) — A top-down, gamepad-playable JRPG in the games hub where the documentation tree is the overworld, real fluorophores are the creature roster, combat is photophysics, and reviewing a page turns a wild room into a villager; layers on the existing review sign-off system.
* ✏️ [PRD-92: Spot finding is not lifetime fitting — a region-property MLE fed by a spot finder that persists its regions](prd-92.md) — `sm_image_mle` segments, measures, gathers photons and fits inside one 897-line core, and **`fit_molecules` re-runs `segment_molecules` itself** — so `segmentation_preview()` returns exactly the labels the fit will use and the fit ignores them, re-deriving a second segmentation that agrees only for as long as every setting reaches both paths. The regions are never persisted, so a segmentation cannot be reviewed, corrected, compared or shared, and none of the spot detection the tree has grown since (`blob_log`/`blob_dog` with their scale space, `difference_of_gaussians`, `white_tophat`, `img_tracking`'s à-trous product) can feed the fit. Split into a `spot_finder` plugin that detects and **writes its regions into the `.pto`**, and a renamed `region_mle` that fits the regions it is handed — "molecule-wise" names the sample, not the operation. The one decision that cannot be deferred: a table of `regionprops` scalars **cannot say which photons belong to a region** (the fit reaches them by `prop.coords` → `tttr_indices`), so the persisted artifact is a **pair** — an `image_data` label raster keyed by a `label` column — and the table copies the burst-companion merge-by-position rule (one row per region *including* the ones the fit skipped, sentinel never absent). Both halves are **batch** tools and the batch half is not a for-loop: there are two batch implementations today — `api/molecule_mle.py::analyze_request` (CLI + RPC, writes the container) and `gui/view_model.py::run()` (GUI, writes a TSV and **no container at all**) — every failure mode is a `continue` that silently shortens the joint table, the joint file lands next to `files[0]`, measurements are keyed by a string path, and the IRF and G-factor are rebuilt from the same measurement once per file. One Qt-free runner replaces both, returning a **run table with one row per input whatever happened to it** (`ok`/`empty`/`failed`/`skipped`) — the sentinel-row rule one level up — with results in each measurement's own container, a cross-measurement table as a derived view rather than a format, IRF preparation once per batch, and cancel. Adds a 3-deep photons → regions → fits chain to [PRD-88](prd-88.md)'s matrix; the estimator half (`Fit2x`/`fit_many`/VV-VH/IRF prep) is deliberately untouched, and the acceptance test is that the split reproduces today's per-region parameters to the last significant figure.
* 🚧 [PRD-93: The clean four-repository split — tttrlib, imp.bff, imp-tricks, chisurf](prd-93.md) — Four repositories hold one stack and until 2026-08-10 nobody could say which owned what; the cost was five days spent recording `AVNetworkRestraintWrapper` as a deleted upstream API while the class sat in the build tree, because imp-tricks' `IMP/bff/restraints` stood in front of imp.bff's and a subpackage resolves to the **first** matching directory and stops. The layering is now **tttrlib → imp.bff → imp-tricks → chisurf**, the placement test is *what is the input* (photons/curves → tttrlib, coordinates → imp.bff, neither → chisurf), and the tiebreaker when input and consumer disagree is *the consumer wins* — which is what keeps **all** of κ² in imp.bff including the anisotropy-fed routes, since reading the input rule literally would split a 762-line file down the middle and separate `kappa()` from the helpers that route shares. Three rules are enforced by tests, each verified to *fail* on a deliberate violation, and a `pre-commit` hook makes "never commit to the IMP checkout" a golden rule rather than a hope. What remains is moving the code: **stage 1** is unblocked and small (delete the dead 283-line `spectroscopy/decay.py` that nothing imports, give the four structure value-classes real `IMP_SWIG_VALUE` declarations instead of a standards exemption, move κ²'s two stragglers out of `chisurf/core/fluorescence/general.py`, upstream the SWIG 4.3 `numpy.i` fix that breaks *anyone* on SWIG ≥ 4.3); **stage 2** moves cgdye — now 11 MB and 67 files after 280 MB of vendored `thirdparty/` was untracked into `junk/` — and is blocked on one decision, whether FRETpredict's rotamer library is data that must ship as IMP module data or reference the rotamer route may skip without; **stage 4** is chisurf's 38,215-line `core/fluorescence`, which gets **no migration at all** and moves by attrition, because a big-bang would break every plugin, notebook and saved project at once during a window in which nothing new works either.
* 📋 [PRD-94: An AV is a Gaussian that means it — mean and covariance from the path-map density](prd-94.md) — imp.bff's `AV` decorator derives from `IMP::core::Gaussian` and `resample()` sets the particle's XYZ to the AV mean, but **nothing ever calls `set_gaussian()`** — every accessible volume claims an identity covariance whatever its shape, and the mean itself is biased because `get_mean_position()` seeds its weight sum at 1.0 unconditionally. The requirement: after every resample the particle carries the density-weighted first and second moments of `PathMap::get_xyz_density()` (source point excluded — the Gaussian describes the dye cloud, not the attachment atom), written through the existing `algebra::get_gaussian_from_covariance` → `core::Gaussian::set_gaussian` machinery, no new math in imp.bff. Consumers: EM/GMM overlap scoring without a bridge, closed-form moment approximations of inter-dye distances as a cheap screen before sampling, and RMF ellipsoid export instead of dumping the grid. Placement per [PRD-93](prd-93.md): coordinates in, so imp.bff entirely.
* 📋 [PRD-95: One serialization story — cereal aligned with IMP, and JSON a human can read](prd-95.md) — imp.bff's cereal port is further along than folklore says — nine headers archive, the SWIG pickle macros are in place, `AVNetworkRestraint` branches correctly inside a single `serialize()` — but **not one field is named**: zero `CEREAL_NVP` in the tree, so a JSON archive emits `value0`/`value1` keys, coupled silently to member order. IMP's own polymorphic registry is hard-wired to binary archives (`object_macros.h:98-107`), so JSON is per-class direct archiving, which the templated `serialize()` functions already permit and the shipped `cereal/archives/json.hpp` already supports — no new dependency. The work: NVP-name every field (binary archives ignore names, so the existing pickle format does not move — and a test holds it to that), mirror `_get_as_binary` with a `_get_as_json`/`_set_from_json` `%extend` macro, commit a golden `.json` per class so member renames fail a test instead of a user, and draw the two-JSONs line — `fps.json` is frozen-key interchange and stays nlohmann, cereal-JSON is object state. `PathMap` stays blocked on `IMP::em::SampledDensityMap` having no `serialize()`; the unblock is an upstream PR, not a workaround here.
* 📋 [PRD-99: Calibrating the cheap model against the expensive one — linker length and rigidity as variables, AV as the thing that must reproduce them](prd-99.md) — An `AV` is a **uniform** distribution over a hard-sphere-accessible volume, with no energetics and a linker that is infinitely flexible by construction; FPSIMP's PMI Monte Carlo is a Boltzmann ensemble with connectivity and excluded volume. **The two have never been asked for the same number on the same structure**, so "the AV approximates the real linker" is a claim nobody in this stack can support, and the nine AV parameters (`AV.h:175-183`) are set by convention — including `contact_volume_trapped_fraction`, shipped as `-1`, i.e. the surface-enrichment correction that exists is switched off. Neither variable that decides whether the approximation holds is a knob today: `run_imp_sampling` takes eight arguments and none is linker length or rigidity (length comes from whatever the AlphaFold segmentation produced; "rigidity" is `plddt_rigid`, a *prediction-confidence threshold* deciding which residues become rigid bodies), and a run costs 100 000 frames × 10 steps, which is why a 6 × 5 × 3 sweep has never been attempted. The expensive side already emits the right observable — `measure.py` computes per-frame distance **and κ²** from four coordinates — so the comparison machinery is half-built, on the far side. The work: declare linker length and rigidity in `parameter_catalog.json` (whose contract test exists *because* a default drifted once and produced a plausible wrong run), add a cheap mode that samples the label subsystem rather than the whole fusion, stop on convergence of the very statistics being compared instead of a guessed frame count, and fit the physically-free AV parameters per cell — dye radii held fixed, since the sweep does not vary the fluorophore. The deliverable is a **map**, not a single fit: the flexible limit where fitted length should track physical length, the stiff limit where the ensemble samples a *shell* that no uniform accessible volume can reproduce (reported as a boundary, not a fudged fit), and the first evidence-based setting of the contact-volume pair. **Precondition:** `set_av_parameter` writes `radius1` into all three radii in the committed tree — fitting radii through that bug would yield a good-*looking* fit with the error absorbed into `linker_length`; the fix is uncommitted in the working tree and must land first. Ordered after [PRD-94](prd-94.md), which supplies the mean/covariance the cheap comparison level needs.
* ✏️ [PRD-100: The accessible-volume stack is re-expressed against IMP, not deleted](prd-100.md) — The `imp` numba route said these files "already migrated to imp-tricks under the same function names — delete the leftover". Checked 2026-08-11: **none of the symbols exist** in `IMP.bff`, `IMP.cgmol`, `IMP.bff.cgdye` or `~/dev/imp.bff`. `IMP.bff.AV` is real, so the direction is right, but ChiSurf's kernels are the *internals* of an AV calculation — this is a migration with a parity bar, not a deletion. Three groups that should land separately: the AV grid, the dye-diffusion/quenching maps, and the ProteinMC potentials (which have **no** IMP target at all and are live behind three GUI widgets). The parity bar is not "tests pass" — those were written against the current implementation — but an exact recorded grid, ⟨R_DA⟩ on 148L, the quenched decay curve-for-curve, and a known-separation simulation, because an AV that is subtly wrong still yields a plausible efficiency and nothing raises. Blocks four allow-list files; **do not start before PRD-97 settles**.
* 🚧 [PRD-98: Acquisition is a stream, not an array — acq on tttrlib's streaming decode, consumers, and sink](prd-98.md) — The acq plugin already decodes BH records with `tttrlib.decode_records` and its carried overflow state, then throws the streaming away: every photon is `np.concatenate`d into one growing array (O(N²) copying, unbounded RAM — "RAM usage monitoring" is a listed *feature*), correlation re-runs the **batch** correlator on the **full history** every 5 chunks so a 30-minute run refreshes slower than a 1-minute one, the MCS trace recomputes all bins to display the last second, **PicoQuant still decodes through a hand-rolled numpy bit-twiddler** the BH path already abandoned, the decode→histogram→correlate pipeline exists **twice** (`DataProcessingThread` and the manager), and `_save_data()` is `pass` — nothing analyzable survives the run unless a raw `.spc` drip was switched on. tttrlib's streaming family shipped **verified** on 2026-08-10 (`StreamingCorrelator` 1.0000 vs batch Wahl on every cascade, `StreamingDecayHistogram` exact vs bincount, chunked-vs-whole equality tested), so the plugin becomes push-based end to end: one library decode path for every device, O(chunk) updates whose cost at minute 30 equals minute 1, bounded RAM, and — when tttrlib PRD-034's native `.pto` sink lands — a self-contained file with clocks and channel map as header tags that exists and grows *during* the measurement and survives a mid-run crash. **Requirements 1, 2, 4 and 5 have landed** (one `PhotonDecoder`, a Qt-free `AcquisitionPipeline` every device and the GUI share, bounded display state, the duplicate manager pipeline deleted), together with the two tttrlib pieces they needed — a new `StreamingIntensityTrace` and numpy typemaps that make a chunk push one C++ call instead of a 1.13 µs/photon Python loop. Requirement 3 (the `.pto` sink) remains blocked on PRD-034.
* 🚧 [PRD-97: FRET docking is imp.bff's — the engine, the AV backend and the one fps.json](prd-97.md) — Stages 0–3 landed 2026-08-11: `IMP.bff.fret` now holds the docking engine, the AV backend (per user rule with **no LabelLib and no numba**), the six algorithm files, and the **single schema-checked fps.json reader** (`fps_schema.py` is the authored union of both dialects with flrCIF item names; `data/fps_json_schema.json` is derived and drift-tested; `pyext/src/fps.py` deleted). ChiSurf's `fret/core` is thin forwarders and its FRET suite is green. The move proved the "agree by coincidence" claim: the C++ reader was writing radius1 into all three AV radii, and ChiSurf's imp-bff AV path had two latent crashes the LabelLib fallback had been swallowing on every call. Stage 4 (measure the three AV paths against each other) remains.
* ✏️ [PRD-96: fpsimp is a consumer, not a second implementation — fluorescent-protein sampling into imp.bff](prd-96.md) — `../fpsimp` is a fifth checkout in the stack and **already drives IMP** (17 `IMP` imports, 3 `RMF`), so this is not a proposal to make it depend on IMP but to stop it re-implementing what imp.bff owns. The duplication is measured and has already cost something: `segments.py` holds the **same four functions** in both trees (`find_fp_domains`, `parse_plddt_from_pdb`, `segments_from_plddt`, `_align_best_window`), and the copies have diverged — imp.bff's alignment now runs on an in-tree Smith-Waterman verified identical to Biopython on 40/40 randomised pairs, while fpsim's still calls Biopython in **11 places** and will never receive that fix. `fp_library.json` has drifted the other way: fpsim carries **mStayGold** and imp.bff does not, with the four shared entries byte-identical — the same table, one copy updated. And `fp_lib.py` shares **no function name at all** between the trees: two accessors over one table, with authority decided by which repository you happen to be in. The line falls on the placement rule rather than filenames — coordinates in → imp.bff, a prediction job or a queue → fpsimp — so `run_imp_sampling`, the FP-domain detection and the FP library move up, while the ColabFold integration, the worker, the web UI and the measurement layer stay. Stage 1 (delete fpsim's `segments.py`) is unblocked and is a strict improvement the day it lands; stage 3 (`run_imp_sampling`, 402 lines) meets imp.bff's own `cgdye/sampling` and gets the same answer as PRD-93's AV duplication: **measure before merging**.
* 📋 [PRD-102: Gigastructures in ChiMOL — pet molecules, instancing, assembly with IMP, and the splat rung](prd-102.md) — **YASARA's performance is the baseline**, stated as such: a presynaptic bouton of **3.6 billion atoms**, interactive on one **RTX 2080**, in over 4 GB of VRAM. That number is reached by two *independent* compressions whose factors YASARA quotes separately — coarse-graining **~50x** and GPU instancing **40-1000x** — and the design consequence is that neither alone is enough: 50x off 3.6e9 is still 72 million, which no immediate-mode renderer draws interactively, so a plan that adopts one and defers the other misses by the factor it deferred. Three parts follow. **Instancing over the copy hierarchy** (one built mesh per distinct type, drawn N times with per-instance transforms) is the larger factor, is independent of the representation, and comes first. **Pet molecules** are YASARA's coarse-grained beads at 1/10 scale and ~2% of the atom count — a *model-building* representation, and YASARA is explicit that pet-world simulation is **not** a faster replacement for all-atom dynamics, a caveat ChiMOL has to carry where a user meets it or someone will publish a pet-world energy; its 1/10 scale is also a second factor of ten waiting to multiply with the scene's existing 10x, which has already produced one 10x-wrong radius of gyration. **Assembly through IMP**, building from a FASTA file with dot-bracket secondary structure, rather than only importing. The NPC demo is today's benchmark and **30x the NPC** is the target; the first move is measuring the NPC's current load time, frame time and VRAM, because every figure here is a ratio against a baseline the repo does not yet record. A **fourth part** was folded in on 2026-08-13: the splat rung as an actual pipeline — scale-space band-limiting so a folded parent does not alias on zoom-out (`Sigma' = Sigma + sigma_filter^2(d) I`), continuous alpha ramps over a screen-space footprint rather than LOD switching, Morton-ordered virtual pages of ~4,096 primitives, a 12–16 byte packed primitive whose position is quantised **relative to its page box** (the same move a planetary renderer makes camera-relative, one level down, and what buys five orders of magnitude out of 16-bit integers), and a WGSL atomic page-request queue with host-side LRU. Four questions are recorded as blocking it: whether the rung serves molecules or replaces them (a splat cannot be selected, measured or labelled), whether Gaussians are folded analytically from structure or fitted as in photogrammetry, whether it composes with instancing since each assumes it owns the draw loop, and the measurement first.
* 🚧 [PRD-101: ChiMOL's interface lives in the viewport — one UI for PyQt and the browser, and the rendering it exposed](prd-101.md) — Every relevant interface, **menus included**, drawn by the renderer inside the 3-D view in Dear ImGui's style, so the desktop app and the browser run one code path and the Qt docks around the view are **deleted rather than ported**. This is a port and not a rewrite because `InternalGui` is already a toolkit-free layout and hit-test engine painting through six operations into GPU quads — Qt is one implementation of the painter, not the substrate — so each remaining panel needs only a body that draws through `Painter`. The one genuinely new mechanism, an **in-viewport window**, has landed: move, corner resize, collapse (button and title-bar double-click), close, raise-on-click, clamped inside the viewport because there is no window manager underneath to retrieve one dragged off the edge; contents arrive through a `body(painter, rect)` callback so the frame knows nothing about what goes in it, and that callback is the seam the remaining four panels hang off. ImGui's **look and window interactions** were taken from its source (32×32 floor, 1 px border, the 4 px grab reach outside the resize corner that makes it catchable, `StyleColorsDark`); its **immediate-mode core was deliberately not** — chimol's chrome is retained, so the IM protocol would mean rewriting every existing panel to gain nothing the quad painter already does, and a second glyph atlas is the duplication the painter seam exists to prevent. `png` hides the windows for its grab; `ray` renders through the tracer and never carried chrome. Remaining: density controls (whose threshold drag is also reported as slow and prone to sticking — the port is the moment to stop rebuilding the isosurface synchronously per mouse move), hierarchy, the prompt's Tab completion (Tab must be claimed before Qt's focus navigation eats it), the menu bar, and the browser chrome that currently **scales with the canvas** instead of staying a constant apparent size. Carries nine separable rendering defects — surface and metaball torn on the shared splat path, mesh ≡ surface, no voxel mode, a volume that survives unload, dark `ray`, dead `show as ribbon` duplicating `trace`, wrong `cell`, dots ignoring zoom, and hidden waters reappearing after a colour-by-atom **whose obvious cause is already ruled out**: every visibility mask is unchanged afterwards, so the fault is in the drawing path, not the colour commands.
* 🚧 [PRD-104: cmtk — ImPlot, ImPlot3D and a view gizmo, drawn through chimol's own painter](prd-104.md) — Ports `epezent/implot`, `brenocq/implot3d` and `Ka1serM/ImViewGuizmo` into chimol's in-viewport toolkit as a new `cmtk` package — cmtk is the toolkit's one name, running on the existing GL/GPU painter rather than a second renderer — extending the painter's six-operation, axis-aligned-rects-only floor with the one primitive none of the three can be drawn without — an arbitrary filled triangle, additive to (not a reversal of) the floor's documented exclusions. Phase 0 (references mined into `junk/`, the triangle primitive on both painter backends) and a first vertical slice land together; full 2-D plot-type breadth, 3-D surfaces/meshes and subplots remain open, tracked in the concept's resume point.
* 🚧 [PRD-105: Engine and car — the compute-placement cleanup of chisurf against tttrlib and imp.bff](prd-105.md) — tttrlib and imp.bff are the engine; chisurf is the car. The rule (compute is C++ and the data stay where the computation is) exists and is enforced; this PRD is the sequenced plan that finishes it — stabilize the seam (director-lifetime segfault, BUG-10, one expression engine), close the measured gaps (curve read off the graph port, data deduplication), put the sampler loop and every model family on the graph or record a written refusal, drive the duplication register (burst search, BVA, regularised inversion ×5, PDA algebra, κ², spectrum algebra) to empty, and batch the chatty per-burst paths — so the C++/Python boundary is crossed once per user action, never per iteration. Supersedes PRD-47; gives PRD-93 stage 4's attrition its priority order; absorbs board T-20260901-05.
* ✅ [PRD-103: A declared UI scheme every plugin is asserted against](prd-103.md) — ChiSurf's two hand-written UI formats are read by loaders that **silently drop what they do not understand**: `load_view_spec` keeps only the keys its section dataclasses declare (`{k: v for k, v in d.items() if k in fields}`) and the tour loader returns an empty tour rather than raising, so a misspelled key is not an error but a control that never appears or a step that points at nothing. Declaring the scheme found **nine defects already shipped** — `rebuild_on_change` on three `value` sections (a `choice` key, and deliberately so), `kind: "combo"` where the key is `style`, `kind: "path"` twice where no such kind exists so both rendered as plain text with no browse button, three more dropped keys, and **nine tour steps using `target.widget`** where the loader reads `target.name`, each rendering as a centred bubble pointing at no control at all, in a guided tour. The scheme is **generated from `_SECTION_TYPES`** rather than transcribed, so it cannot drift; `additionalProperties` is false everywhere with two named carve-outs; both schemes dispatch on a discriminator rather than `oneOf`, because a `oneOf` failure names the wrong section type in the one line a plugin author reads. The generalisation that matters is beyond files: **a second reader of a shared format is asserted from its own side** — chimol builds painted forms from the same specs and its first draft invented `{"type": "float", "min": 0}` for the real `{"type": "value", "kind": "float", "minimum": 0}`, which read there and was valid nowhere else. 128 specs, 38 tours, and 127 manifests are asserted, one test per file, with negative controls for each defect that had shipped. The scheme is published for editors via `.vscode/settings.json` and `$schema` keys; custom-section options are validated per factory key; `manifest.json` has its own generated schema; and `generate_starter_view_spec` produces a valid `view.json` from a model's parameter groups.
* 📋 [PRD-118: FcsMdf node — the MDF diffusion mode fits through the graph](prd-118.md) — A bff Node wrapping the already-ported FcsMdf kernel so `GeneralFCSModel`'s `"mdf"` mode and `MdfFCSModel` stop refusing the graph; the relaxation/background factors stay a downstream `Expression`, and the builder learns the producer-node pattern the decay path already uses. The kernel's quadrature cache must survive across evaluations or the graph measures slower than the director.
* 📋 [PRD-119: FCS kinetics "full" mode as a node over the FcsSaturation kernel](prd-119.md) — The numerically integrated, power-dependent mode stops crossing per iteration: a node over `fcs_saturated_curve_shape` with the rate scheme as fixed configuration (a free rate refuses — a matrix port is real work not built speculatively). Lands after PRD-118, reusing its builder hook; the `K[target, source]` fixture trap and the vacuous all-zero-curve agreement are the recorded hazards.
* 📋 [PRD-120: the bounds transform must not stall on decade-spanning bounds](prd-120.md) — The shared minimiser's bound handling collapses the LM step when bounds span decades (`ub=1e9` defaults): fits *finish*, plausible-looking and wrong (chi2r ≈ 600 where unbounded scipy reaches 1.0 in six evaluations, graph and director identically). Fix once at the engine; the ICS test's bounds-off workaround and the known-issues entry are deleted together with it.
* 📋 [PRD-121: census hardening — a curve that was never computed must not read as buildable](prd-121.md) — Third census column (is `update_model`'s curve non-degenerate), because the census's dangerous rows are silent passes: `Mfd2DModel` no-ops, `FidaModel` computes on a meaningless axis without complaint. The FIDA axis becomes a refusal, the `pch_mixture` defect goes upstream with the warning-names-the-bug contract.
* 📋 [PRD-122: core/math forwarder slice — Richardson–Lucy, polymer rdf, the convolution straggler](prd-122.md) — Three duplication-register rows with unambiguous engine owners become parity-pinned forwarders; the two rows blocked on owner decisions (the `i0` transposed digit, the `distance_between_gaussian` name collision) are staged visibly behind their decisions, not silently skipped.
* 📋 [PRD-123: burst kernels onto tttrlib — search, BVA/2CDE, and the per-burst slicing loops](prd-123.md) — Forwarding the duplicate burst kernels and batching the per-burst `TTTR`-construction loops land together, because either alone keeps half the problem. Companion outputs must stay byte-identical — the column-wise merge contract makes silent misalignment, not failure, the risk; one row per burst including the skipped ones.
* 📋 [PRD-124: one regularised inversion — five Tikhonov/MaxEnt copies onto the engine](prd-124.md) — One chisurf-side seam maps every caller's objective convention (the QuickFit σ≈1 renormalisation bit once already) onto tttrlib `Nnls`/`MaxEntQp`; respects the queued owner decision that keeps the cached-SVD loop the `fcs_maxent` default until the 2–3× engine cost is accepted.
* 📋 [PRD-125: one Gaussian-mixture EM — three in-tree copies, one kernel](prd-125.md) — `ml/mixture`, `fret/accurate._em_1d` and the HMM M-step collapse onto one engine kernel, with the HMM's known trap cases (−inf−−inf, dead-state 0/0, frozen Dirichlet zeros) pinned against it; coordinated with PRD-77 so the M-step does not fork a fourth time.
* 📋 [PRD-126: finish the κ² move — 794 scalar-loop lines onto IMP.bff.spectroscopy.kappa2](prd-126.md) — Two functions already shim; the rest of the file's scalar Monte-Carlo orientation loops follow, through the engine's central RNG, with distribution-shape parity (moments and tails, not means). A half-forwarded file is the worst state — the seam exists, so divergence between the halves is invisible.
* ✅ [PRD-127: PDA algebra dedup — the numpy PDA copies beside the tttrlib calls](prd-127.md) — `convolve_pch` and `burst_log_likelihood_reference` had no production callers and moved to `test/` as named frozen references; `mfd/histogram.py`'s per-burst nested sum audited and found not a `tttrlib.Pda` duplicate (per-burst-conditioned vs. global S1S2). S1S2 cache defect re-verified intact, outside this PRD's files.
* 📋 [PRD-128: MFD spectrum algebra onto the graph's own producers](prd-128.md) — `mfd/patterns.py`'s lifetime spectra, Perrin and polarised patterns re-implement what `FretSpectrum`/`AnisotropySpectrum` already compute for the TCSPC graph. VV *and* VH parity with rotations armed; the `1 + 2r0` normalisation trap is already documented at the decay builder — reuse its settings, do not rediscover them.
* 📋 [PRD-129: correlator batching — the fFCS matrix in one call, g³ where the engine owns it](prd-129.md) — `species_filtered_correlation` stops walking the same photon stream n(n+1)/2 times; one tttrlib entry emits the full species auto/cross matrix. The higher-order kernel gap, if real, is filled upstream first; `second_order_correlation`'s nested loop then forwards.
* 🚧 [PRD-130: GopichSzabo stops rebuilding — persist the engine, fit on the Minimizer](prd-130.md) — Engine persistence and the eigendecomposition-guard trim are done (139 constructions → 1 per fit, answers unchanged); `fit()` onto `bff.Minimizer` remains blocked on tttrlib exposing per-burst log-likelihoods.
* 📋 [PRD-131: fit_many everywhere — the per-row fit() loops in imaging and MFD](prd-131.md) — `img_pixel_mle`, `region_mle`, the MFD per-cell loops and `pixel_maps.py`'s per-window `CLSMImage` constructions move onto fit2x's GIL-released batch entry, row-identical including failed-fit sentinels; per path, the process pool's continued existence is then a measured decision, not an inheritance.
* 📋 [PRD-132: the DEER bootstrap band through the graph — 120 scipy fits become 120 graph runs](prd-132.md) — `compute_uncertainty`'s replica re-fit is a fit, and fits have a graph route: one graph per band, the target swapped per replica, scipy kept as the standard refusal fallback. Statistical band parity at matched seed plus point parity of one replica; the Tikhonov re-inversion is out of scope on purpose.
* 📋 [PRD-133: fallback audit — confirm the kept Python twins are never the live path, then delete](prd-133.md) — Six fallbacks kept "by design" (BOCPD, recurrence, Gopich–Szabo, PCH/FIDA, blind IRF, MaxEnt) are dispositioned one by one: proven unreachable and deleted, or proven live and promoted to warn-naming-the-gap with a test that exercises the fallback path itself — an untested fallback is worse than none.
* 📋 [PRD-134: document the seam — a fitting-subsystem refresh and a concept for the graph objective](prd-134.md) — The busiest seam in the tree (`core/fitting/minimizer.py`: builders, refusal contract, keepalive lifetimes, cache key, curve publication, the model protocols) gets its subsystem concept, and `okf/subsystems/fitting.md` stops describing the pre-migration world; PRD pointers in seam code re-point at the concept per the no-PRDs-in-source rule.
