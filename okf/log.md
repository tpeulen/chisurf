# Update Log

## 2026-07-15
* **Maximum-likelihood (2I*) fitting: shared tttrlib `fit2x` harness + first-class in-core Poisson objective.** Two coupled changes that make MLE a proper part of ChiSurf's fitting infrastructure instead of siloed plugin boilerplate. (1) New Qt-free harness `chisurf/core/fluorescence/mle/` (`fit2x.py`): `Fit2xSettings` (dt/period/IRF/background/polarisation), a reusable `Fit2x` fitter (builds the tttrlib `Fit23`/`Fit24`/`Fit25` once, fits many decays), a typed `Fit2xResult` with named accessors (`tau`/`gamma`/`r_scatter`/`r_experimental`, `as_dict`), and `assemble_jordi` for the parallel|perpendicular layout. It is the single seam around tttrlib's Maus-2001 `2I*` MLE — the estimator itself is already good C++; only ChiSurf's harness around it was poor (the raw `tttrlib.Fit23(dt=…, p2s_twoIstar_flag=…)` construction + `res['x']`/`res['twoIstar']` parsing was copy-pasted across four call sites). Migrated the burst-MLE batch worker (`burst_mle_analysis/_mp_worker.py`), the wizard's batch pass, and `microscopy/sm_image_mle` onto the harness; the wizard interactive-preview and the `img_pixel_mle` monolith still call raw `Fit23` and are the next consumers to move. (2) Added a selectable `noise_model` on `Fit`: `"default"` keeps weighted-least-squares (Neyman) exactly as before; `"poisson"` (aliases `mle`/`2istar`) returns the **signed Poisson deviance residuals** `sign(μ−y)·√(2·(μ−y+y·ln(y/μ)))` from the new `deviance_residuals` helper, so `Σr²=2I*` and the *existing* `leastsqbound` LM engine, `chi2`/`chi2r`, covariance, F-test and residual plots all minimise the Poisson likelihood unchanged (Laurence & Chromy 2010) — the correct estimator for low-count TCSPC/FLIM/burst decays. Threaded `Fit.noise_model → Model.get_wres → calculate_weighted_residuals`; PDA models and every default fit are byte-unchanged. Tests: `test/fluorescence/test_mle_fit2x.py` (harness: Jordi assembly, settings validation, `Fit23` lifetime recovery, fitter reuse) and `test/fitting/test_poisson_mle.py` (deviance≡2I* identity, zero/perfect-bin handling, selector plumbing, WLS back-compat, Poisson fit recovers params with reduced 2I*≈1) — 12 pass (arm64). No regressions (the 4 pre-existing `test_fit.py` standalone-invocation failures fail identically on baseline). Docs updated: [subsystems/fitting.md](/subsystems/fitting.md), [plugins/burst.md](/plugins/burst.md). This is phase 1 of a larger effort (H2MM/FRETBursts benchmarks in docs; FRET-2CDE + DCBS-in-tttrlib feeding ndxplorer) — see the `mle-fretbursts-benchmark-resume` memory.
* **FCS model catalogue expansion (PAM port, A/B-verified) + FLCS filter fixes.** Ported PAM's (Schrimpf 2018) FCS fit-model catalogue into `chisurf/core/models/fcs/models.yaml` as 13 absolute-`D`, physically-parametrised models (x in s; D µm²/s; w µm; tau µs): `FCS_D`, `FCS_D_2Component`, `FCS_D_2exp`, `FCS_TauD`, `FCS_TauD_anomalous`, `FCS_flow`, `FCS_D_Background`, `FCS_D_afterpulsing`, `FCS_TauD_Bleaching`, `FCS_2Focus_D` (two-focus absolute `D`), `FCS_FRETFCCS` (2-state kinetics), `nsFCS_ab_cd_triplet`, `FCS_Scanning`. Each carries the γ=1/√8 prefactor and triplet term. **A/B-verified**: `test/fitting/test_fcs_pam_ab.py` transcribes each PAM MATLAB `fit` lambda verbatim (keeping the `1e-12`/`1e-6` SI factors, which cancel in the diffusion terms → reduced `4·D·x/w²`) and asserts equality to the ChiSurf equation over random parameters (evaluated through the real `ParseModel` scanner). This replaced the initial hand-rolled `w0/s` two-focus entries. A catalogue-wide parse test also caught and fixed **3 pre-existing broken entries** (`3D Gauss, 1 exp. bunch, 1 stretched exp. bunch` — unbalanced parens + missing `a1`/`td1`; `Autocorrelation with biexponential anisotropy term` — `td`→`td1`; `1x2D Gauss, 1x3D Gauss, 1 bunching + 5 anti-correlation` — missing `s`), whose equations referenced parameters absent from their `initial:` blocks. **FLCS filters** (`chisurf/core/fluorescence/fcs/filtered.py`, PAM `Calc_fFCS_Filters` as reference): the weighted-pseudo-inverse algebra was already correct; added `rcond`/`tikhonov` conditioning + `filter_condition_number` diagnostic (for near-collinear/similar-lifetime patterns), `uniform_pattern` (flat afterpulsing/dark-count species → afterpulse-free FLCS), and `photon_filter_weights` (per-photon micro-time→filter weight, the core for wiring filters into a weighted correlator); fixed the legacy `calc_lifetime_filter` divide-by-zero (zero-bin guard) and removed its nonstandard global renormalisation that broke the `Σ_t R_k(t)·p_j(t)=δ_kj` relation. Tests: `test/fitting/test_fcs_pam_ab.py`, `test_fcs_2ffcs.py`, `test_fcs_filters.py` — 31 pass (arm64 env). Remaining gaps (correlator wiring of filters, channel-aware par/perp weighting, scatter/IRF & model-generated pattern species, non-Gaussian Dertinger MDF, and the pCF/SCCF/MIA models) documented in the new OKF concept [references/fcs-pam-port.md](/references/fcs-pam-port.md); [references/two-focus-fcs.md](/references/two-focus-fcs.md) and [plugins/fcs.md](/plugins/fcs.md) updated. PAM cloned under `junk/PAM` as reference.
* **Two-focus FCS (2fFCS) model functions + status doc.** Added two absolute-diffusion model functions to `chisurf/core/models/fcs/models.yaml` — `2f-FCS auto-correlation (absolute D)` and `2f-FCS cross-correlation (absolute D)` — written in the `D`/`w0` parametrisation (Dertinger 2007). The cross-correlation carries the known inter-focus distance `d` in an `exp(-d²/(w0²+4Dx))` term, so a global fit sharing `D`/`w0`/`s` (with `d` fixed) yields an **absolute** diffusion coefficient without a separately calibrated volume; it correctly peaks at a finite lag and reduces exactly to the auto-correlation at `d=0`. Verified through the real `ParseModel` scanner (params resolve to `N,D,w0,s,d,b`); headless test `test/fitting/test_fcs_2ffcs.py` (5 tests) asserts the physics. Documented what's implemented vs missing in a new OKF concept [references/two-focus-fcs.md](/references/two-focus-fcs.md) (missing: Dertinger's non-Gaussian modified detection function, which needs `erf` — unavailable in the string-equation evaluator's NumPy-only scope, so it needs an `erf` binding or a coded FCS model class; plus focus-distance calibration, a dedicated 2f channel-pairing preset, a guided global-fit GUI, triplet/multi-component variants, and an end-to-end absolute-`D` recovery test). Linked from [plugins/fcs.md](/plugins/fcs.md) and the references index.
* **New plugin `burst_ebfret` — binned-trajectory smFRET HMM (ebFRET port).** Closes the one confocal/TIRF gap the burst group lacked: analysis of **binned intensity-vs-time FRET traces** (wide-field/camera data) rather than photon streams. Added `chisurf/plugins/burst/burst_ebfret/` — a Qt-free, NumPy/SciPy-only port of ebFRET (van de Meent et al.; a re-implementation of vbFRET): `core/vbem.py` (per-trace variational-Bayes EM over a univariate Gaussian-emission HMM — Dirichlet priors on initial/transition, Normal-Gamma emission prior in ebFRET's `(m,β,a,b)` form, linear-scaled forward-backward), `core/ebayes.py` (empirical-Bayes outer loop with conjugate h-step prior updates ported from ebFRET's `+dist/{dirichlet,normgamma}/h_step.m` — Dirichlet Newton + Normal-Gamma moment-matching), `core/viterbi.py`, `core/analysis.py` (K-scan + evidence selection + dwell/transition decoding), `io.py` (ebFRET stacked `.dat` loader), `api/models.py`, `backend/services.py` (`burst_ebfret.jobs.compute` RPC), `cli/main.py` (`ebfret compute`). **Validated against ebFRET's own `simulated-K04-N350` fixture** (vendored under `tests/data/`, 350 traces / 39,595 frames): at K=4 the port recovers `[0.03, 0.34, 0.51, 0.62]`, the two well-separated interior states within 0.05 of ebFRET's fitted `[0.33, 0.516]` — endpoints differ only because this port fits the uncorrected proximity ratio `A/(D+A)` whereas ebFRET fits a background-subtracted signal (donor-only near 0). Tests: 7 fast synthetic unit tests + 4 recovery/integration tests (3 `slow`) all pass (arm64 env; pixi env solve is currently broken on an unrelated `msgpack` candidate). Porting this one algorithm covers the functionality of four MATLAB-only TIRF tools on fret.community (**ebFRET, vbFRET, HaMMy, SMACKS**). Scope: single-prior D=1 path; prior-mixture subpopulations, SMD/session I/O, and a GUI tool are follow-ups. Marked `experimental`/`menu_hidden`. Grew out of a fret.community/software gap analysis vs ChiSurf (repos cloned into `junk/`). Other gaps assessed (not built): diffusion-FRET photon simulation is already substantially in **tttrlib** (`~/dev/tttrlib`, PRD-005 Burbulator port — 3D Brownian + PSF + FRET + TCSPC + CLSM, SPC-132 byte-exact; remaining: periodic/mirror BCs, Photon-HDF5 write, single-molecule throughput) and needs only surfacing via chisurf **PRD-53**; rFRET ratiometric + FLIM crosstalk correction can reuse the existing forward crosstalk-matrix builder (`plugins/core/lightpath_simulator/backend/simulator.py::get_crosstalk_matrices`), which is currently never inverted to correct measured data; wide-field camera-movie TIRF (spot localization/trace extraction) remains a genuine large gap but is pure-intensity and off ChiSurf's photon-centric path (only PAM's TIFF-stack phasor-FLIM ingest is FLIM-relevant). See [plugins/burst.md](/plugins/burst.md).
* **Extracted MMFDB into a self-contained sibling repository.** `modules/mmfdb` (178 in-tree files) was moved out to `~/dev/mmfdb` as a standalone, independently versioned package destined for `github.com/Fluorescence-Tools/mmfdb`, and chisurf now consumes it via a **symlink** `modules/mmfdb -> ../../mmfdb` (the established `imp-tricks` pattern). This leaned on chisurf being pre-wired for the split: the `sys.path` bootstrap (`chisurf/_bundled_packages.py`) resolves `modules/mmfdb/src` through the symlink unchanged, `pyproject.toml` already declares `mmfdb==0.1.0` and excludes it from `packages.find`, and `test/core/test_mmfdb_vendor_package.py` still passes (8/8). **New repo** (`~/dev/mmfdb`, `git init`, 202 files, 631 tests pass, docs build `-W` clean): dropped `EXTRACTION.md` + the stray `examples/None`; added own `.gitignore`, `MANIFEST.in`, `.github/workflows/ci.yml`, an `examples` extra (`tttrlib`/`fretbursts`/`phconvert`) and moved `test_min_client.py` into `tests/`; reframed `README.md` and fixed all `modules/mmfdb/…` doc paths to package-relative; host-neutrality still guarded by `tests/test_architecture.py`. **chisurf side:** removed `test/plugins/mmfdb/` (moved) and the redundant root `docker-compose.mmfdb.yml`; added an mmfdb sibling-clone/install step to `rattler-recipe/{build.sh,build.bat}` and the three CI workflows (closes the prior gap where the packaged app installed every local module *except* mmfdb); synced the `mmfdb_admin` manifest (added the pre-existing-undeclared `fluorophores.import_default_set`). Verified end-to-end through the symlink: vendor boundary, burst-workflow facade, H2MM-simulation, and notebook discovery all pass. **Examples decision:** the 8 chisurf-based notebooks stay in mmfdb with chisurf as an optional extra; docs render them non-executed (`nb_execution_mode="off"`), so the mmfdb doc build needs no chisurf. Nothing pushed — creating/pushing `Fluorescence-Tools/mmfdb` and the coordinated chisurf commit are left to the user.
* **MMFDB slop cleanup.** Removed AI-generated cruft from `modules/mmfdb`: deleted a stray 950 KB SQLite DB accidentally written to `examples/None`; trimmed docstring puffery ("comprehensive parser" in `schema/pdbx_metadata.py`, "first-class read service" in `provenance/lineage.py`); and deleted six provably-dead legacy getters on `SampleDefinition` in `src/mmfdb/models.py` that had zero callers repo-wide.
* **MMFDB: drop the dual-field legacy sample model (pre-release, no back-compat needed).** Removed the entire flat/structured legacy layer that duplicated the flrCIF/PRD-02 model. `ProbeDefinition` lost `position`/`position_label`/`chain_id`/`residue_name` (and their `__post_init__` mapping to `seq_id`/`comp_id`/`asym_id`/`auth_name`); callers now pass flrCIF fields directly. `SampleDefinition` lost `entity_name`/`entity_sequence`/`entity_type`, the flat `donor_probe_name`/`donor_position`/`donor_position_label` (+ acceptor equivalents), and the deprecated structured `donor`/`acceptor` `ProbeDefinition` fields — everything now goes through `entities`/`probes`/`fret_pairs`. `_validate_vocabulary` validates each entity's `entity_type` (still hard-rejects unknowns with the same "Invalid entity_type" message) instead of a flat field. Migrated consumers: `samples/sample_manager.py` (`_metadata_from_definition`, `_collect_all_probes`, `_insert_entity`, `_derive_fluorophore_types`, `_get_probe_description`, `_sample_type`/key-values now read the first entity's type; probe `description` sources `auth_name` not `position_label`), `samples/sample_requests.py` (`SampleCreateRequest`/`SampleUpdateRequest` de-legacied), `admin/backend/services.py` `_normalize_probe_payload` (still accepts `residue_number`/`residue_name`/`chain_id`/`position` input aliases, emits only flrCIF keys), and `chisurf/gui/widgets/sample_picker.py` (builds `probes=[ProbeDefinition(...)]` + a `FretPairDefinition` from the donor/acceptor dye fields instead of the flat kwargs — dyes are now recorded as real sample probes with correct donor/acceptor roles). Updated `test/fio/test_sample_manager.py` and `modules/mmfdb/tests/test_prd19_collapse.py` to the new API. Verified: mmfdb standalone suite **628 passed / 2 skipped**, `test/fio/test_sample_manager.py` **21 passed**, mmfdb_admin plugin tests green, plus an end-to-end round-trip (FRET sample → DB → correct donor/acceptor roles, `residue_number`/`asym_id`/`residue_name` positions, conditions). Three `test/fio/test_dataset_browser.py` failures are pre-existing (auth/object-store env, fail on baseline without these changes). See [architecture/mmfdb.md](/architecture/mmfdb.md).

## 2026-07-13
* **General non-programmer facade for the MMFDB burst workflow.** Added `chisurf/plugins/burst/burst_analysis/api/workflow.py` — a Qt-free `BurstWorkflow` facade that collapses the whole remote pipeline (start/connect MMFDB server + login → register dataset(s) → fetch-by-handle → burst selection → BVA → H2MM) into one plain-English line per step, so a bench scientist needs no settings dataclasses, object UUIDs, or TTTR bookkeeping. `BurstWorkflow.demo()` spins up a private in-process server for tutorials; `.connect(url, user, password)` targets a real deployment. Generality: a declarable `Setup` of named `Detector`s (routing channels + optional micro-time windows), built via `Setup.from_channels(green=…, red=…, yellow=…)` for any number of dyes and mirroring ChiSurf's `{name:{chs,micro_time_ranges}}` model. FRET is not baked into the setup (no donor/acceptor or "two-colour" naming) — the pair is chosen per analysis (`bva("green","red")`, `h2mm(donor=…, acceptor=…)`), defaulting to the first two detectors, while H2MM models every detector as a stream (true N-colour). Optional/inferred `file_type`; `register_all` + multi-dataset `select_bursts`; and a selectable search `method` (default `"burst"` sliding-window, which — unlike the old `filter_active=False` grouping — actually honours `min_photons`). `Bursts.bva()` / `.h2mm(states=…)` return `Bva`/`H2mm` results carrying `.plot()`, `.summary()`, `mean_proximity_ratio`, `dynamic_fraction`. Rewrote `modules/mmfdb/examples/mmfdb_00_overview.ipynb` (executes end-to-end: 3 datasets, 488 bursts, BVA 41 % dynamic fraction, H2MM 3-state fit) and `test/plugins/burst/test_mmfdb_burst_workflow.py` (8 tests against a live demo server: handle round-trip, multi-dataset selection, explicit-vs-default setup, three-colour setup with a chosen FRET pair, micro-time-gated detectors, setup validation, BVA ranges, deterministic 2-state H2MM).
* **MMFDB standalone (no-lock-in) example, notebook testing, click CLI, docs.** Added `modules/mmfdb/examples/mmfdb_08_standalone_no_lockin.ipynb`: uses `mmfdb` (embedded `MFDatabase`) with **tttrlib + FRETBursts** and **no `chisurf` import at all** (asserted), proving MMFDB is usable standalone and combines libraries without lock-in — verified by executing it with only `modules/mmfdb/src` on `PYTHONPATH`. Added `test/plugins/burst/test_mmfdb_example_notebooks.py` — executes all example notebooks (00–08 + the H2MM plugin example) as a `slow`-marked regression guard via `python -m nbconvert` in a subprocess with `DYLD_*` stripped (chisurf's bootstrap points them at the base env's lib, which shadows the child's zlib and breaks Pillow); 9/9 pass. Converted the external CLI `external_tools/fret_burst_tool.py` from argparse to **click** (mandate). Added a **standalone Sphinx project** at `modules/mmfdb/docs/` (own `conf.py` — MyST + `furo`, a `Makefile`, a `docs` extra in `modules/mmfdb/pyproject.toml`, and `_build/` gitignored) so the docs travel with the package when it becomes its own repo; the chisurf docs tree no longer references them. The complete page set is `index`, `installation` (pip-first, `mmfdb serve` CLI), `concepts` (the data model: object store, artifacts, operations, provenance DAG, mmCIF, embedded-vs-server), `usage` (task→tested-notebook map), `rpc-clients`, and the pre-existing `webadmin`. The docs are **framework-agnostic**: the pages describe the software without addressing reader personas (an earlier draft leaked the audience personas into the prose and addressed "you PhD student / PI" directly — removed), and chisurf-centric framing was scrubbed throughout (chisurf now appears once, as *one optional consumer among many*). Also fixed the package framing: `pyproject.toml` description no longer says "used by ChiSurf" (now "Multimodal fluorescence metadata and provenance database …"), the wrong `numpy<2.0` pin relaxed to `numpy>=1.21` (mmfdb runs under NumPy 2), and the README reframed as host-neutral. The example notebooks are a proper **myst-nb gallery**: `conf.py` uses `myst_nb` (`nb_execution_mode = "off"` — the notebooks already carry test-run outputs), a `docs/examples` symlink exposes them to the source tree, and `index.md` lists them with a `{toctree}` glob (`examples/mmfdb_*`) instead of hand-coded links — each renders as a full page with its stored plots, and `usage`/`concepts` link them with `{doc}`. **Verified `sphinx-build -W` builds all 17 pages (6 docs + 9 rendered notebooks) with zero warnings.** `rpc-clients.md` is a **tight wire-contract spec** (JSON-RPC 2.0 over `/rpc`, bearer-token auth, `/objects` upload/download, method namespaces) so an agent can implement a client in any language without reading chisurf; it `literalinclude`s a ~50-line stdlib-only reference client `modules/mmfdb/examples/external_tools/mmfdb_min_client.py`, kept honest by `test/plugins/mmfdb/test_min_client.py` (login + object round-trip + JSON-RPC call/error against a live chisurf-free server). Filed **INC-09**: mmfdb is packaged standalone and works without chisurf, but the only RPC client (`MMFDBClient`) + example facade (`BurstWorkflow`) live inside chisurf and `mmfdb.api` requires auth in-process, so a chisurf-free consumer must use the embedded repository; target is mmfdb as its own repo with a Qt-free client. See [specs/assessment.md](/specs/assessment.md).
* **MMFDB external-tool provenance + deposition examples (+ two gap findings).** Added `modules/mmfdb/examples/mmfdb_06_external_tool_provenance.ipynb`: a standalone vanilla-`tttrlib` CLI (`external_tools/fret_burst_tool.py`, run as a real subprocess) **and** FRETBursts (installed; needs a one-line `np.asfarray` NumPy-2 shim) both process one MMFDB-stored measurement, each recorded via `mmfdb.v1.operations.record_with_artifacts` (software name/version, command line, typed params, checksummed I/O artifacts) and shown as a queryable lineage DAG — both recover the ground-truth mean E≈0.5. Added `modules/mmfdb/examples/mmfdb_07_deposition.ipynb`: a full **export → ZIP → re-import** round-trip — `archive.zip.export` packs a self-contained bundle (DB snapshot + provenance graph + manifest + native raw in `external_data/`), we add a standards **mmCIF** metadata file (`export_flr_cif`), and a *second* MMFDB instance is reconstituted from the ZIP alone (verified: same artifacts + provenance + native raw); deposition to Zenodo/OSF is left to their own Python tools per the ZIP. Renamed the overview to `mmfdb_00_overview.ipynb`. Filed two backlog findings: **DATA-05** (external-tool runs aren't first-class — no `command_line`/`exit_code` columns, no `external_tool` op type, inconsistent op_type validators) and **DATA-06** (deposition is export-only — no `archive.zip.import`, bundler reads `file_path` not the object store, mmCIF export is FLR-only). See [specs/assessment.md](/specs/assessment.md).
* **MMFDB example suite across modalities.** Added `modules/mmfdb/examples/mmfdb_0{1..5}_*.ipynb` — one runnable, self-contained tutorial per modality, all following the same simulate-with-tttrlib-0.27 → store-in-MMFDB → fetch → analyse pattern (each executed with embedded plots): **burst_selection** (burst-size/duration histograms), **bva** (static-vs-dynamic scatter + proximity-ratio histograms), **h2mm** (ground-truth recovery + dashboard), **fcs** (single diffusing species → `SimEngine` SPC → `tttrlib.Correlator` autocorrelation → diffusion time `τ_D=w0²/4D`), and **imaging** (CLSM raster via `SimSystem`/`SimScanner`, raw marker-annotated event stream stored losslessly as `.npz` in the object store — HDF5/PTU drop markers — then `CLSMImage` reconstruction, r≈0.99). FCS/imaging use `BurstWorkflow` purely as the MMFDB gateway (`register` + new public `fetch(handle)→Path`). The overview `mmfdb_00_overview.ipynb` now also demonstrates `simulate()` + ground-truth validation and the H2MM dashboard. See [architecture/mmfdb.md](/architecture/mmfdb.md).
* **Ground-truth simulation + richer H2MM for the burst facade.** Extended `BurstWorkflow` with `simulate(fret=…, exchange_rate=…, …)` — builds a tttrlib 0.27 confocal-simulator config (`SimEngine.from_dict` → `run` → `to_tttr` real SPC-132; FRET via per-channel brightness `q=[(1-E)b, Eb]`, exchange via `k_nrad`), writes it, registers it in MMFDB, and returns `Simulation(handle, setup, truth)` so H2MM recovery can be checked against the defined states (verified: 2-state dynamic → E=[0.25,0.75], 3-state static → [0.2,0.5,0.8], exact). Enriched the `H2mm` result with the model-selection `scan` (n_states/loglik/BIC/ICL), canonical plots (`plot_states`, `plot_model_selection`, `plot_transitions` rate matrix, `plot_dwell_times`, and a 2×2 `plot()` dashboard), physical time base (`macro_time_resolution`), and selectable `engine`/`criterion`. Added `chisurf/plugins/burst/burst_h2mm/examples/H2MM_02_MMFDB_GroundTruth.ipynb` (simulate → MMFDB → recover → validate; BIC/ICL + engine modes; static-vs-dynamic proximity-ratio histograms mirroring tttrlib's `fret_smfret.py`) and `test/plugins/burst/test_mmfdb_h2mm_simulation.py` (5 tests: ground-truth recovery, scan, mode agreement, plot smoke). Upgraded the main mmfdb notebook's H2MM cell to the dashboard. Facade `plot()` on the H2MM result is now the dashboard (was the FRET bar → `plot_states`). See [plugins/burst.md](/plugins/burst.md) and [architecture/mmfdb.md](/architecture/mmfdb.md).
* **Seed ChiSurf's default fluorophore spectra into a served database.** Added `ProbeMixin.import_default_spectra` (`modules/mmfdb/src/mmfdb/queries/probes.py`) which loads the canonical dyes ChiSurf ships in `data/default_fluorophore_spectra.json` (Cy3B, ATTO 647N, Alexa Fluor 488, Cy3, Cy5, Trp, 2-aminopurine, …) as first-class `chisurf_default`-sourced probes with optical properties + absorption/emission curves; idempotent (reuses existing rows). Exposed as RPC `fluorophores.import_default_set` (`admin/backend/fluorophore_services.py`) and a 🧪 **Load defaults** toolbar button on the web-admin optical-components page. Verified against the live server (8 probes, 12 spectra, 31 optical properties; Cy3B plots both curves). Added `modules/mmfdb/tests/test_default_spectra.py` (seed/decode/idempotency). See [architecture/mmfdb.md](/architecture/mmfdb.md).
* **Web-admin gains a 1:1 optical-components curation surface.** Ported ChiSurf's Qt `OpticalComponentDock` to the standalone MMFDB web-admin (`modules/mmfdb/src/mmfdb/webadmin/optical_components.py`, routes `/optical-components`, `/optical-components/action`, `/optical-components/duplicates` in `webadmin/app.py`). Full parity: component-type tabs (fluorophore/filter/dichroic/detector/light-source), curation toolbar (📥 import ref set, ✅ approve, ❌ reject, 🤖 AI triage, 🔍 review queue, 👯 find duplicates), searchable status-filtered checkbox table with status badges, overlaid spectrum plot rendered as **inline SVG (presentation attributes only, no JS)** so it satisfies the `default-src 'self'` CSP, a read-only detail panel, and a duplicate review/merge page. Moved two pure-Python helpers into the standalone package so the web UI has no ChiSurf dependency: `admin/backend/triage_checks.py` (default `deterministic_checks`, now wired into `register_services`) and `admin/backend/duplicate_grouping.py` (name/abs/em Bayesian grouping + union-find). Spiced the whole web UI with emoji nav/headings per request. Verified headlessly via an in-process WSGI drive: login → list/filter → 2-probe SVG overlay (polylines+legend) → detail panel → approve/reject/triage (persisted) → duplicates find (15 groups) → merge (probe count 2288→2287). See [architecture/mmfdb.md](/architecture/mmfdb.md).
* **Connect ChiSurf to the standalone MMFDB Docker server + surface connection info.** MMFDB now ships a Docker image (`modules/mmfdb/Dockerfile`, `docker-compose.mmfdb.yml`) running `mmfdb serve` (WSGI HTTP + JSON-RPC on `:8080`). ChiSurf's `MMFDBClient` already had a `remote` HTTP transport; verified it connects end-to-end to the containerized server (login + status over `/rpc`). Documented switching to it via a commented `mmfdb.client: {mode: remote, base_url: ...}` example in `settings_chisurf.yaml`. Extended the `mmfdb.status` RPC (`modules/mmfdb/src/mmfdb/admin/backend/services.py:status_handler`) to also report `database_dialect` (sqlite/postgresql), a password-redacted `database_location`, and `object_store_backend`. The mmfdb-admin **Overview** panel (`plugins/core/mmfdb_admin/gui/tool.py`) now shows a "Connection" block — client mode (embedded/remote/in-process), endpoint, database type, and object store — and the status bar is prefixed with the mode. Tests: extended `modules/mmfdb/tests/test_webadmin.py` (status envelope asserts new fields) and added two GUI tests in `mmfdb_admin/test/test_admin_navigation.py` (overview shows mode/DB type; label helpers reflect remote/PostgreSQL). Verified against a live `docker compose` server: remote client reports `dialect=sqlite`, `/var/lib/mmfdb/mmfdb.db`, object store `local`, schema 44.

## 2026-07-12
* **Rebrand MFDB → MMFDB (multimodal fluorescence database).** Full identifier rename across the tree: the Python package `modules/mfdb/src/mfdb` → `modules/mmfdb/src/mmfdb` (dist name `mmfdb`, `mmfdb-admin` console script, all `from mmfdb.*` imports), the `mmfdb_admin` plugin, `chisurf/gui/widgets/mmfdb`, `chisurf/core/mmfdb_services.py`, `chisurf/core/project/mmfdb_adapter.py`, the plugin `api/mmfdb.py` modules (burst/tttr/ndxplorer/spectra_downloader/_dev), the `mmfdb_flr_ext.dic` dictionary, tests, docs, and OKF concepts (`architecture/mmfdb.md`, `specs/mmfdb.md`, `references/mmfdb-*`, `plugins/profiles/mmfdb-admin.md`). **Data schema renamed too:** mmCIF categories `_mfdb_*` → `_mmfdb_*`, the `category_prefix`/`_MMFDB_TABLE_PREFIXES`, and every SQL table `mfdb_*` → `mmfdb_*`. Added schema **v42** migration (`SCHEMA_VERSION` 41 → 42): `_rename_legacy_mfdb_tables` runs up front in `migrate_schema` — before any reconcile migration — renaming existing `mfdb_*` tables in place (idempotent; drops only empty stale targets), so live databases keep their data; the `_schema_version` fallback keeps old DBs reporting v41 so the migration fires. Verified: synthetic v41 DB + shipped `spectra.db` (v40) migrate with data preserved and no leftover `mfdb_*` tables; standalone mmfdb suite **499 passed**, `mmfdb_admin` **111 passed**, fluorophore_db **33 passed**, chisurf core/fio/plugin mmfdb tests green. (Note: `grep -I` silently skipped several UTF-8 files as "binary" during the bulk pass — CLAUDE.md, which is gitignored, and the `_dev/fluorophore_db` files — caught and fixed via a per-file scan.)

## 2026-07-11
* **MMFDB prerelease architecture hard cut.** Made `modules/mmfdb` a real host-neutral package and reduced the surface before release: `import mmfdb` now exposes only `MFDatabase` plus runtime configuration (lazy repository import instead of 123 eager exports); all ChiSurf dependencies were removed from `src/mmfdb` and locked with an AST guard; all ChiSurf callers were cut over to direct `mmfdb.*` imports and the `chisurf.core.mmfdb` facade/meta-path aliasing was deleted. ChiSurf-specific fit serialization/parameter-name lookup, Burst execution, deterministic fluorophore triage, and pipeline browsing now enter through explicit callable injection in `chisurf/core/project/mmfdb_adapter.py` and `chisurf/core/mmfdb_services.py`. Added one request-scoped `APIRequestContext` so `mmfdb.v1.*` opens/authenticates exactly one database and no longer mutates dispatcher params. Schema repair is now explicit v41 migration work: fresh databases build the current declarative baseline directly, existing v40 databases reconcile once, current opens have no bootstrap/reconcile side effects, and `readonly=True` is enforced by SQLite. Removed shadowed duplicate methods, dead materialized-view code, private repository utility re-exports, and prerelease setup-definition aliases; corrected the GUI client's duplicated lifecycle/reagent namespaces and aligned the manifest with registered auth methods. Updated the package/extraction architecture, completed PRD-24, and refreshed the cleanup assessment. Verification from the finished tree: standalone **481 passed**; ChiSurf `test/fio` **237 passed, 12 skipped**; MMFDB Admin **111 passed**; architecture/schema **15 passed**; additional MMFDB integrations **21 passed**; Ruff Pyflakes/import checks + compileall + `git diff --check` pass; standalone wheel builds successfully.

## 2026-07-10
* **Burst — H2MM experimental flag now actually shows in the workflow.** The `manifest.json` `experimental: true` flag only drives the nav banner when H2MM is launched as its *own* plugin; embedded as **step 6 of the `burst_analysis` shell** (how it's normally used), the `NavigationPanelTool` reads the *panel dict*, which lacked the flag — so no ⚠/banner appeared. Added `experimental: true` + `experimental_message` to the H2MM entry in `burst_analysis`'s `BURST_PANELS`, so the step label renders `6. H2MM  ⚠` and a warning banner is prepended to the panel. Reverted the earlier status-label hack (it got overwritten by "Data folder: …"); kept the standalone-window title suffix. New guard test `test_h2mm_panel_is_flagged_experimental`; burst_analysis + GUI tests green.
* **Burst — H2MM re-affirmed experimental.** Kept the `manifest.json` `experimental: true` flag (drives the NavigationPanelTool banner + nav ⚠) and refreshed its stale `experimental_message` (it claimed "not yet cross-validated against H2MM_C" — now A/B-validated on *simulated* data, but still not on measured experimental smFRET, and the GUI defaults to the approximate float32 engine). Added a visible in-window marker (title suffix `⚠ experimental` + an amber status note) so the standalone tool signals its status even when opened outside the navigation panel. Treat results as preliminary.
* **Burst — H2MM GUI: per-iteration progress so long single fits don't look stuck.** A real SPC-132 dataset (`bh_spc132_sm_dna/…burstwise_All 0.1500#60`) appeared to "hang on 1/3 state counts". Diagnosed: not hung — with unscaled ~µs macro times the data has **139,013 unique inter-photon Δt** (`n_slots`), so each EM map rebuilds that many `A^Δt`/ρ propagators → **171 ms/map → ~85 s for one k=2 fit** (k=1 is trivial, so the bar sat at 1/3 while k=2 ground). Fix: threaded a per-EM-map `on_iter(done, max_iter)` callback through `optimize`→`_squarem`/`_plain_em`→`fit_states`(restart-aware)→`fit_one`→`scan_states`, so the scan reports **fractional** progress (completed fits + fraction of the current fit). The GUI now advances the bar smoothly with a formatted **ETA** (s/min/h), throttles per-iteration ticks to ~10 Hz, and refreshes the live plots only on fit completion. Also added a log hint recommending a higher `time_scale` when `n_slots` is huge (`time_scale=100` → 3042 slots, ~46× faster; physically appropriate for ms-scale FRET vs 50 ns macro resolution). Lint clean; GUI + engine tests green.
* **Burst — H2MM GUI: non-blocking fit + ETA progress + live plots; numba CPU scale-up + fastest default.** Made the H2MM GUI responsive and fast. The fit now runs **off the UI thread** (`chisurf.gui.widgets.progress.Worker` on the global `QThreadPool`) behind an `EnhancedProgressDialog` showing a computed **ETA** and a Cancel button (cancellation via a `threading.Event` checked in the progress callback → `_FitCancelled`). The model-selection (BIC/ICL) and FRET-state plots **update live** after each state-count fit, driven by a new `progress(done, total, fits)` callback threaded through `analyze`→`scan_states`→`run_analysis`. The GUI now defaults to the **fastest method** (`em-float32` engine + `patience=1` early-stop). Critically, **scaled up numba threads**: `env_bootstrap`/shipped `settings_chisurf.yaml` pinned `numba_num_threads: "1"`, so the whole parallel engine ran single-threaded in-app — changed the shipped default to `"auto"` (resolved to CPU-cores − 1; MKL/OMP stay 1 to avoid oversubscription) and flipped the local `~/.chisurf` value; verified it now resolves to 7 on this 8-core machine. Removed the old modal `_ProgressDialog`. New GUI tests (fastest-default, live-scan-plot helper, non-blocking worker end-to-end via a monkeypatched `run_analysis` that still drives progress); **all burst_h2mm tests green**; lint clean.
* **Burst — H2MM plugin polish: engine select, defined ndX output, CLI/GUI, notebook, docs.** Rounded the H2MM plugin into a finished tool. **Engine selection** end-to-end: `H2mmSettings` gains `engine`/`refine_iters`/`patience`/`surrogate_path`/`write_photons`; the GUI settings tab has an engine dropdown + scan-patience spinner, the `h2mm compute` CLI has `--engine`/`--surrogate`/`--patience`/`--refine-iters`/`--no-photons`, and both flow through `analyze`. **Defined output openable in ndxplorer**: `core/export.py` builds a **per-photon** table (Viterbi `State` + macro/micro time, channel, stream) and a **per-burst** summary, written as `<folder>/h2mm/h2mm_photons.h5` (pandas key `results`) + `h2mm_bursts.csv` — plain numeric tables with a `Mean Macro Time (s)` axis that ndX opens directly; `photons.py` extended with `PhotonMeta` + `return_meta` so per-photon macro/micro/channel align with the engine layout. **Notebook** `examples/notebooks/H2MM_01_Simulated_smFRET.ipynb` (linear, matplotlib, no `__main__`): simulate smFRET via `tttrlib` → extract → fit/select → Viterbi trajectory → engine comparison → ndX export; validated end-to-end via `nbconvert --execute`. **Docs** `docs/H2MM.md` (theory: photon-by-photon HMM, the `A^Δt` variable-Δt trick, Baum-Welch, BIC/ICL, Viterbi; engines; outputs; CLI/API) registered in `docs/index.rst`. New `tests/test_export.py` (meta alignment, table schema/numeric-only, HDF5/CSV round-trip). **30 burst_h2mm tests green** (incl. GUI instantiation with the new dropdown); lint clean.
* **Burst — H2MM model-selection early-stopping + SIMD/scan-trick dead-ends recorded.** Explored further *algorithmic* speed-ups now that per-fit exact EM is at the roofline. (1) **Early-stop the state-count scan** (`scan_states`/`analyze` opt-in `patience`): the BIC/ICL curve is U-shaped and the over-fit high-`k` fits are the *most expensive* — a redundant state makes a flat likelihood ridge so they run to `max_iter` without converging (measured: k=1..5 on 2-state data took 5/24/500/500/500 maps). Stopping once the criterion has risen for `patience+1` counts gave a **measured 1.65× faster scan** with identical selection; `patience=None` (default) is unchanged. (2) **SIMD confirmed a dead end by experiment:** reordering the forward matmul from a `k`-reduction to a supposedly-SIMD-friendly `i`-inner AXPY made it **0.3–0.7× (slower)** at n=2..5 — LLVM already register-optimises the small unrolled reduction, and over-burst SoA batching is structurally blocked by H2MM's variable-Δt design (each burst/photon needs a *different* transition matrix → per-lane gather, no uniform SIMD op). Not adopted. (3) Skipping per-`k` Viterbi for BIC selection is negligible (<1 % of a fit) — not adopted. New early-stop test; lint clean. Documented in [burst plugins](/plugins/burst.md).
* **Burst — H2MM memory-bandwidth optimisations + unified engine selector.** The E-step is memory-bound, so two cache/bandwidth tricks (both numerically identical — A/B-vs-`H2MM_C` still exact): (1) **cache locality** — `α`/`scale` moved from a shared `(N, n)` array to small **per-thread scratch buffers** (sized to the longest burst, reused across bursts, hot in L1/L2), eliminating the largest DRAM stream. A **controlled isolated A/B** at 2.4M photons (one EM map) measures **1.25–1.6× on the E-step** (biggest at `n=3`), plus a *flat* ~10 ns/photon from 240k→2.4M (the old array caused a large-`N` cliff) and tens-of-MB lower peak memory. (2) **narrow integers** — `streams`→`int8`, `gap_slot`→`int16` (from `int32`): shrinks the index-array footprint ~3× but, once `α` is out of DRAM, is **speed-neutral** (isolated A/B 0.94–1.11×, within noise). **Correction:** an earlier note here claimed ~38 % from the int shrink at `n=2` — that was measurement noise on a loaded machine; the controlled A/B shows the cache-locality buffers are the real win and the int shrink is kept for footprint, not throughput. Confirmed SIMD is not worth a rewrite (state dim n=2–5 too small; over-burst SoA vectorisation eaten by memory bandwidth) and float32 already captures the easy width/bandwidth ~2×. Added a single **compute-engine selector** `core/engines.py` (`em`/`em-float32`/`surrogate`/`surrogate-refine`) threaded through `fit_states`(new `single_precision`)/`scan_states`/`analyze`; surrogate engines fall back to exact EM when no trained model is supplied, and model selection always scores by BIC/ICL. 19 burst_h2mm tests green; lint clean. (CLI/GUI engine wiring, ndX-openable per-photon output, notebook + docs still to land.) Documented in [burst plugins](/plugins/burst.md).
* **Burst — optional amortised (surrogate) neural estimator for H2MM (PRD-60, draft).** After proving the remaining H2MM speed-up can only come from cutting EM's iteration count — and that *initialisation can't do it* (seeding Baum-Welch from the exact generative parameters still took ~39 maps vs ~42 random; only starting from the dataset's own MLE hit the ~6-map floor, because EM's cost is the finite-sample last mile) — added a **simulation-based-inference** fast path that *replaces* EM: `core/surrogate.py` trains a small scikit-learn MLP once on simulator draws to predict `(prior, trans, obs)` in one forward pass. POC: on held-out 2-state data the surrogate recovered per-state FRET to **0.028–0.032** mean-abs-error vs **0.047–0.052** for a single EM fit (more accurate — it's a Bayes estimator under the training prior — and faster, the gap widening with N). Module is Qt-free, `numpy`+`scikit-learn` only (already a dep), permutation-invariant features (windowed-FRET histogram/quantiles + photon-lag autocorrelation + Δt stats, versioned), canonical-ordered/`log10`-transition encoding, fast O(photons) training simulator, `save`/`load`, and an optional `refine_iters` EM polish. Wired as opt-in `fit_states(surrogate=…, refine_iters=…)` (default `None` → EM unchanged); ships **disabled, no pretrained model**, EM fallback when scikit-learn absent. Approximate — for over-determined data/exploration, not final numbers or model selection. Also concluded (measured) that GPU offload is a dead end here: on the M1 Pro, jax-metal couldn't compile the forward-backward `lax.scan`, and a single Metal batched-matmul step (0.786 ms) already ≈ numba's entire 240k-photon forward — the tiny-state sequential recurrence can't use a GPU. 5 surrogate tests; **26 burst_h2mm tests green**. New concept [PRD-60](/prds/prd-60.md); documented in [burst plugins](/plugins/burst.md).
* **MMFDB — metadata→edge materialization + resolvable audit labels (ELN crosslinking lessons).** Studied a mature ELN's information-management/crosslinking model and adopted two provenance-integrity wins. (1) A sample/experiment key-value whose value is an `mmfdb://<node_type>/<node_id>` reference now **materializes an idempotent `mmfdb_edge`** (`linked_to`, tagged with the originating metadata key) from the owning node to the referenced node — the ELN pattern "a metadata field that references another entity *is* a link" (`queries/artifacts.py::parse_node_ref`/`_materialize_metadata_ref`, wired into `set_sample_key_value`/`set_experiment_key_value`). (2) Edge/link creation audit entries (`add_edge`, `record_operation_link`) now carry a **resolvable target label** (the node's human title via `_resolve_node_label`), mirroring the ELN's changelog anchors. New `tests/test_metadata_edges.py` (5). Confirmed MMFDB's single polymorphic `mmfdb_edge` is architecturally ahead of the ELN's ~25-table per-pair junction matrix (do not copy it), and its `.dic`-driven vocab beats freeform extra_fields for a databank. Deferred as an **idea for later**: an optional polymorphic **tags** layer (scoped, AND-searchable) — recorded in [ELN crosslinking lessons](/references/eln-crosslinking-lessons.md). Standalone **471 passed, 1 skipped**; `test/fio` **236 passed** (1 pre-existing).

## 2026-07-08
* **Burst — H2MM Viterbi parallelised over bursts; state-splitting evaluated and rejected.** (a) `viterbi()` still ran a serial Python loop over bursts while the E-step was parallel; added `_viterbi_all` (a `prange` mirror — each burst decodes a disjoint `path[s:e]` with its own work arrays, path log-liks reduce), **≈3.2× faster** (6.8→2.1 ms on 300k photons, identical paths) and on the hot path of every model-selection scan (ICL) and final decode. (b) Tried warm-starting the multi-state scan by **state-splitting** the converged `(k-1)`-state fit (SMEM-style): empirically **unsafe** — a single split cannot undo the state merging in the smaller fit, so on well-separated 3-state data it reached a *worse* k=3 optimum (−251346 vs random −236009) yet a large gain-over-parent made naive "trust" heuristics accept it; the only safe variant (augment + always take best) is *slower*, not faster. Reverted the scan changes and removed the `split_model` helper; documented the negative result in `scan_states` and [burst plugins](/plugins/burst.md). A robust version needs full split+merge SMEM (out of scope). **21 burst_h2mm tests green.**
* **MMFDB — collapsed the auth provider registry to fixed local+ldap dispatch (PRD-59).** No auth providers beyond local + LDAP are planned, so the open extension mechanism was removed: deleted `register_provider`/`build_provider`/`available_providers`/`ProviderContext`/`_PROVIDER_FACTORIES` from `security/auth_providers.py`, and `login.resolve_provider` is again a fixed `local`/`ldap` dispatch (unknown → `AuthError`); the local-always-available fallback now constructs `LocalAuthProvider` directly. The `AuthProvider` protocol + `LocalAuthProvider`/`LdapAuthProvider` remain (the protocol is the seam if a provider is ever needed). Swapped the three registry tests in `test_auth_hardening.py` for a `resolve_provider` dispatch test; updated PRD-59 (removed OIDC/SAML + external-IdP as non-goals → "no further providers planned"), `architecture/mmfdb.md`, and HANDOVER. Standalone **466 passed, 1 skipped**; auth suites green. Advances [PRD-59](/prds/prd-59.md).
* **Burst — H2MM engine: four compounding speedups (E-step fusion, SQUAREM, spectral build, float32).** Second optimisation pass on `core/h2mm.py`, each gated by the A/B-vs-`H2MM_C` tests. (1) **Two-pass E-step:** folded the γ (occupancy) and W (transition) accumulation into the single backward sweep and replaced the `(N, n)` β array with two `(n,)` vectors (β[n] needs only β[n+1]) — halving the large-array memory traffic; ~3–9 % faster (bigger at low `n`). (2) **SQUAREM acceleration** (`optimize(accelerate=True)`, default): squared-extrapolation EM (Varadhan & Roland 2008 S3) with a monotonicity safeguard reaching the *identical* fixed point in far fewer maps — 2-state **81→33** maps (2.4× faster), 3-state **807→132** (6× faster) on well-separated data. (3) **Spectral cache build** (`_build_caches_eig` + `_fill_caches`): for sparse photon streams (`n ≥ 3`, `max Δt ≥ 512`) `A^Δt`/`ρ` come from an eigendecomposition via divided differences instead of binary exponentiation (1.4–2.4× faster build at `max_dt≈2200`), conditioning-guarded with a pair-power fall-back; dense data keeps pair-power. (4) Opt-in **`single_precision`** float32 hot loop + caches (~1.9× at 3 states), explicitly approximate. Also fixed a latent landmine: ChiSurf's `env_bootstrap` rewrites `NUMBA_NUM_THREADS` *after* numba's pool launches, which crashed the first late cold compile (the float32 kernel) via numba's reload guard — `optimize` now re-pins the env to numba's launched pool size (`_sync_numba_threads`). New engine tests (SQUAREM≡plain-EM + fewer maps, eig≡pair-power build, eig declines on defective `A`, float32 near float64); **21 burst_h2mm tests green** (stable across repeated full-dir runs). Net vs `H2MM_C` on the slow benchmark: **0.51×** (2-state) / **0.52×** (3-state) wall-time per map, now also converging in fewer maps. Documented in [burst plugins](/plugins/burst.md).
* **Burst — H2MM A/B *performance* guard vs `H2MM_C`.** Added `test_benchmark_vs_reference` (2- and 3-state) to `tests/test_ab_vs_h2mm_c.py` as a companion to the correctness A/B tests: it times a full EM run of the numba port against the reference C `H2MM_C` on identical tttrlib-simulated data, reports **ms/EM-iteration** (JIT warmed up first, so compilation isn't charged; normalised per iteration so convergence-count differences don't skew it), and asserts (a) the two engines reach the same optimum and (b) the port stays under ~1.5× the reference time/iter — a wide-margin regression guard that would catch an `O(N·n⁴)`-style slowdown. Marked `slow` (excluded from default runs; run with `-m slow -s`). Measured on 150k-photon dense bursts: **0.68×** (2-state) / **0.50×** (3-state) the `H2MM_C` wall-time per iteration. Documented in [burst plugins](/plugins/burst.md).
* **Burst — H2MM E-step now beats the reference `H2MM_C` (defer the ρ contraction).** The parallel E-step (`core/h2mm.py::_estep`) formed the transition tensor ξ *per gap* — an `O(N·n⁴)` inner loop that also streamed the whole `rho_cache` from memory once per photon — leaving the numba engine ~1.05–1.21× *slower* than `H2MM_C` (gap widening with `n_states`). Restructured to accumulate only the per-unique-Δt weight `W[slot,k,m] = Σ_gaps α[n,k]·obs[m,y_{n+1}]·β[n+1,m]/c` in the hot loop (`O(N·n²)`, thread-local), then contract `ξ[i,j] = Σ_{slot,k,m} W[slot,k,m]·ρ[slot,k,m,i,j]` **once per slot** in the serial reduction (`O(n_slots·n⁴)`, `n_slots` = unique inter-photon Δt ≪ N). Mathematically identical (ρ is fixed per slot, so the per-gap sum factors out); `rho_cache` is now read `n_slots` times instead of N. On 2000×120-photon simulated data (100 EM iters) the engine went from ~1.05×/0.95×/1.12× the `H2MM_C` wall-time at 2/3/4 states to **0.61×/0.37×/0.28×** — i.e. ~1.6×/2.7×/3.6× *faster*; the 4-state fit dropped 1253→334 ms. All 10 A/B-vs-`H2MM_C` + engine tests still green (converged logL/BIC/params and Viterbi match the reference to the same tolerances). Documented in [burst plugins](/plugins/burst.md).
* **MMFDB — threaded + enforced `auth` across the whole `api.py` boundary (INC-04).** Most `api.py` functions ran unauthenticated and lacked an `auth` parameter — yet **all ~40 are registered as `mmfdb.v1.*` RPCs invoked via `h(**params, auth=auth)`**, so those versioned methods would have `TypeError`'d the moment they were called with a token: a latent break *and* the enforcement gap. Added `auth` to every one (samples/experiments/operations/parameters/graph/setups/chinet/branches/audit) with shared helpers for **progressive enforcement**: `_acting_user_id` (authenticated principal, else configured default — preserves in-process/GUI), `_new_object_acl` (writes stamp the owner + a default ACL: samples/experiments join the existing artifact/operation ACLs), `_acl_read_or_pass`/`_acl_filter_or_pass` (reads enforce ACLs *where they exist*, stay open otherwise — no legacy lockout), and `_effective_target_user` making the user-scoped branch ops **self-or-admin** (fixed an IDOR where any user could set/read another's active branch). New `tests/test_api_auth.py` (5: auth accepted + owner-ACL + cross-user filtering, graceful no-ACL reads, in-process default-user attribution, IDOR blocked, admin-may-target-other). Standalone **468 passed, 1 skipped** (was 463); `test/fio` **236 passed** (1 pre-existing). Marked [INC-04](/specs/assessment.md#inc-04) 🚧 ADDRESSED. (Landed alongside a concurrent `mmfdb.admin`→`mmfdb_admin` extraction in the tree; committed only the `api.py`/test changes.)
* **Burst — new H2MM photon-by-photon HMM plugin (`burst_h2mm`).** Added a single-molecule FRET plugin that resolves sub-burst state dynamics on the microsecond scale, ported from the Pirchi *et al.* 2016 / `H2MM_C` algorithm into a **self-contained numba engine** (no external H2MM dependency). `core/h2mm.py` caches `A^Δt` and the transition-count `ρ` tensor once per **unique** inter-photon Δt via associative pair-power binary exponentiation (cost scales with photons, not clock ticks), runs a scaled forward-backward + Baum-Welch EM, and provides Viterbi+ICL, BIC, a model factory and a validation simulator. Follows the client-server standard: Qt-free `core/` (h2mm/photons/analysis) → RPC `backend/services.py` (`burst_h2mm.jobs.compute`) → thin `H2mmClient` + `H2mmTool` GUI (FRET states, transition-density plot, BIC/ICL model selection, per-state dwell-time distributions) + click CLI. Consumes `.bur` bursts sliced from `tttrlib.TTTR`, mapping routing-channel + micro-time windows to photon streams. Wired into `burst_analysis` as **step 6 "H2MM"** with workflow-context handoff (inherits burst folder + channel defs). Marked **experimental** (numba engine validated on simulated ground-truth recovery, monotonic logL, BIC selection, Viterbi, RPC, stream-gating, headless GUI). **A/B-verified numerically equivalent to the reference `H2MM_C`** on tttrlib-simulated data carried through the real `tttrlib.TTTR` + plugin extraction path: fixed-model and converged log-likelihood/BIC agree to ~1e-9, π/trans/obs to ~1e-7 (2- and 3-state), and Viterbi paths match 100% over ~36k photons (`tests/test_ab_vs_h2mm_c.py`, skips if `H2MM_C` absent). 15 tests green; not yet validated on experimental data. Documented in [burst plugins](/plugins/burst.md).
* **MMFDB Admin boundary correction — API/CLI stay in `mmfdb.admin`, GUI split to `mmfdb-admin`.** Corrected the prerelease admin ownership line: `mmfdb.admin` remains the admin API/CLI package (RPC service implementation, direct in-process API client, CLI/`mmfdb-admin` console script), while the former ChiSurf plugin GUI now lives in sibling package `modules/mmfdb-admin` as the standalone `mmfdb-admin-gui` Qt surface. The ChiSurf `core/mmfdb_admin` plugin is now a shim: manifest services point to `mmfdb.admin.backend.services`, and GUI construction delegates to `mmfdb_admin.gui.app`. Ported a ChiSurf-free AutoForm layer into `mmfdb-admin` and wired browse tabs to AutoForm-backed row detail panes. Added boundary coverage for the split and smoke-verified standalone CLI status plus offscreen GUI construction.
* **MMFDB — auth polish: external-user password guard + auth-services lint cleanup (PRD-59).** Rounding out the auth layer: `change_password` (both the token-based `auth_services.change_password_handler` and the legacy `password_services.change_password_handler`) now **rejects external-provider users** — their credential lives in the directory/IdP, so writing a local hash was meaningless (and confusing). Cleaned the pre-existing lint debt in `auth_services.py` (9 → 0), including a **real latent bug** — the `require_handler_auth` return annotation referenced undefined `sqlite3`/`Principal` (F821; harmless only because `from __future__ import annotations` stringifies it) — plus removed 5 genuinely-unused re-imports (verified nothing imports them from that module) and sorted imports; same for `password_services.py`. 1 new regression test. Standalone **462 passed, 1 skipped**; `test/fio` **236 passed** (1 pre-existing). Deliberately **not** done (low value, noted): user-enumeration timing equalization on the local path — network jitter dominates PBKDF2 timing and MMFDB isn't internet-facing.
* **MMFDB — closed a critical auth hole + enforced "local always available" (PRD-59 security review).** Auditing "what else needs doing," found that an **external (LDAP) user could be logged in via the local provider with an empty password**: JIT-provisioned directory users have no local `password_hash`, and `LocalAuthProvider`'s (and legacy `password_services.login_handler`'s) "no hash → accept when no password" branch treated them as passwordless — `login(provider="local", user_id="<ldap_uid>", password="")` succeeded without the directory. Fixed by making **both** local-auth paths reject users whose `auth_provider != 'local'` (genuine local passwordless users are unaffected). Also found a **lockout risk**: with LDAP as the configured default, the bootstrap **local admin couldn't log in** (default dispatch sent everyone to LDAP). Added a **local fallback** in `login()` — when a non-local default returns `None`, the local provider is tried — making "Local always available" real; it can't weaken security because the local provider now rejects external-homed users (a directory-rejected external user still fails). Third finding: **email-match could hijack a real local account** — an LDAP user whose email collides with an existing local admin was silently relinked (`auth_provider→ldap`), handing control to whoever holds that directory account. Now `resolve_or_provision_user` only email-adopts a **placeholder** local account (local-homed, no password); a real local-credential or other-provider account is never converted (JIT mints a distinct account instead). 4 new regression tests (external-user local-login blocked incl. empty password; local admin logs in under LDAP-default; external user can't sneak through the fallback; email collision doesn't hijack a local password account). Standalone **461 passed, 1 skipped** (was 457); `test/fio` **236 passed** (1 pre-existing).
* **MMFDB — auth hardening + extensible provider registry (PRD-59 Phase 4).** Made the auth layer excellent + extensible (per the "great arch, extend later" directive). **Provider registry** (`register_provider`/`build_provider`/`available_providers` + `ProviderContext` in `security/auth_providers.py`): the sole extension point — a new backend (OIDC, external-IdP, …) is a class + one registration call; `login.resolve_provider` now dispatches through it and never needs editing (built-in `local`/`ldap` self-register). **Fixed a real security bug:** failed RPC logins weren't persisted (each `login_handler` opens its own connection and the old flow raised without committing), so `is_throttled` never accumulated and brute-force throttling was silently disabled — `login._record_failure` now commits the attempt (empirically 0→≥5 persisted across connections). **Directory-authoritative reconciliation** (`sync_identity`): external-provider logins refresh `is_admin`/`email`/`display_name` and reconcile the provider's new `AuthIdentity.managed_groups` (add carried, remove absent; admin membership follows `is_admin`) while never touching locally-managed groups. **LDAP hardened:** `base_dn` validation, `try/finally` unbind, optional TLS CA (`ca_cert`→`ldap3.Tls`), and unreachable/search errors → clear `AuthError` (wrong password stays `None`). New `tests/test_auth_hardening.py` (9: registry extend/build/unknown, throttle-persists-across-RPC-connections, attribute+group reconciliation incl. de-escalation and unmanaged-group safety, LDAP missing-base-dn / directory-unavailable / managed-groups). Standalone **457 passed, 1 skipped** (was 448); `test/fio` **236 passed** (1 pre-existing).
* **MMFDB — auth CLI + config surface + PRD-59 concept (PRD-59 Phase 3).** Added a headless `mmfdb-admin auth` command group (`admin/cli`): `login --user … [--password …] [--provider local|ldap]` (mints a session token via `login()`), `whoami --token …` (resolves a token to its principal), and `status` (shows the active provider + LDAP settings) — all with `--json`. Exposed a standalone `mmfdb-admin` console-script entry (`[project.scripts]`) so the path is headless-first without depending on ChiSurf's `csc` plugin mounting. New `tests/test_auth_cli.py` (4 tests via click's `CliRunner`: login→whoami round-trip, bad-password abort, invalid-token, `status` defaults local). Wrote the **PRD-59** concept (`okf/prds/prd-59.md`, status in-progress) + index/glyph and an auth-provider section in `okf/architecture/mmfdb.md`. **Correction:** Phase-1/2 log bullets and code docstrings mislabelled this work "PRD-49" (which is the unrelated Multiparameter-Fluorescence PRD); renumbered to the next free id **PRD-59** across code/tests/log (Phase-1/2 commit *messages* keep the old label — history). Standalone **448 passed, 1 skipped** (was 444); `test/fio` **236 passed** (1 pre-existing). This completes the local + LDAP + CLI scope; OIDC/SAML and eLabFTW-IdP remain deferred.
* **MMFDB — LDAP authentication provider (PRD-59 Phase 2).** Added `LdapAuthProvider` to `security/auth_providers.py`: search+bind against a directory (service-account bind → search `user_filter` under `base_dn` → re-bind as the located user DN to verify the password), with `memberOf` → MMFDB group mapping (`group_map`) and admin elevation (`admin_groups`). The `ldap3` dependency is **optional + lazy** (`_require_ldap3` guard; new `[ldap]` extra in `pyproject.toml`), and the provider takes an injectable `connection_factory` so it's driven **fully offline via `ldap3` MOCK_SYNC** in tests (no live directory). LDAP-filter values are escaped (`escape_filter_chars`) against injection. Config wiring: `config.set_auth_config_resolver`/`configured_auth_config` (host resolver → `MMFDB_AUTH_PROVIDER`/`MMFDB_LDAP_*` env → `None`=local), consumed by `auth_services._auth_config`; `login.resolve_provider` already dispatches `"ldap"`. Secret handling: `security/credentials.py` gained `store/load_ldap_bind_password` (keyring, distinct service) so the bind password lives in the OS store / env, never settings JSON. New `tests/test_ldap_auth.py` (10 tests via MOCK_SYNC: attr+group mapping, wrong/empty password, unknown user, service-bind-failure → `AuthError`, filter-injection escaping, missing-dep `ImportError`, `login()` JIT-provision + group-sync end-to-end, and the env/resolver config path). Standalone **444 passed, 1 skipped** (was 434); `test/fio` **236 passed** (1 pre-existing). CLI + PRD-59 concept are Phase 3.
* **MMFDB — pluggable authentication framework + Local provider (PRD-59 Phase 1).** MMFDB auth was 100% local and hardcoded (`auth_services.login_handler` → `verify_password` → `create_session`, no provider seam). Introduced a small provider layer under `security/`: `auth_providers.py` (`AuthIdentity` dataclass, `AuthProvider` `Protocol`, `LocalAuthProvider` wrapping the existing PBKDF2 `verify_password` with byte-for-byte the same admin/passwordless/no-hash rules), and `login.py` — a `login(conn, provider=…, user_id, password, …)` orchestrator that authenticates → **resolves/JIT-provisions** the MMFDB user via new `flr_sample_users.auth_provider`/`external_id` columns (match by `(auth_provider, external_id)` → `email` → JIT-create for external providers; `local` returns the row as-is) → **syncs mapped groups** into `mmfdb_group_member` → mints the existing session. `auth_services.login_handler` now delegates to it (RPC contract `{ok, authenticated, token, expires_at, user}` unchanged; the `mmfdb_admin` plugin copy is already an `import *` re-export, so nothing to reconcile). Schema: added `auth_provider TEXT DEFAULT 'local'` + `external_id TEXT` to `flr_sample_users` (non-canonical table, so the hardcoded DDL is authoritative) + index `idx_users_external(auth_provider, external_id)` for reverse lookup; **fixed the hardcoded fixed-salt bootstrap-admin password** (`_hash_admin_password`) to a per-user random salt (`admin` still verifies; two fresh DBs now differ). New `tests/test_auth_providers.py` (13 tests). Standalone suite **434 passed, 1 skipped** (was 421); `test_mmfdb_auth.py`/`test_mmfdb_user_management.py` green (behaviour preserved); `test/fio` **236 passed** (1 pre-existing ndxplorer fail). PRD-59 concept to be written in Phase 3; LDAP provider is Phase 2.
* **MMFDB — swept the trivial SELECTs in `repository.py` core onto the DAO + deleted more schema-mismatched dead code (PRD-26).** Extended the `queries/` SELECT sweep into the repository core. Converted the trivially-keyed reads to `dao.get`/`list`: `validate_extensible_vocab`, `get_external_file`, `get_experiment_full` data, `get_raw_data_references`, `get_parameters`, the object content-md5 read, the two `get_processed_data_products`/`trace_processed_data` operation-artifact reads, and the whole `export_flr_cif` fan-out (probes/positions/distances/forster/external_files/optical_properties/spectra/struct_ref[_seq]) — dropping a **dead `SELECT * FROM flr_sample` whose result was discarded** and the now-unused `_row_dict` helper. Also folded `add_audit_log`'s raw `INSERT`+`last_insert_rowid()` into `dao.insert`. **More dead code found & deleted:** `get_chemical_descriptors`/`add_chemical_descriptor` and `get_images`/`add_image` — the live `chem_descriptors` (`id,descriptor_type,descriptor,program,program_version`) and `images` (`id,probe_id,image_name,image_data,image_format,…`) tables have **completely different columns** than these methods write/read (`probe_id`/`value`/`image_path`/`image_id`/`descriptor_id` don't exist), so they'd raise on any call — zero callers, so deleted (yesterday's `dao.insert` conversions of them were unreachable). `optical_properties` is live & used, so its methods stayed. Left raw (the legitimate home): the `get_photon_streams` JOIN, access-control/impact subqueries + `COUNT`s, `get_audit_logs`/`struct_ref_seq_dif` compound `ORDER BY`, and the two `*_key_value`/`optical_properties` column-projection reads. Standalone **421 passed, 1 skipped**; `test/fio` **236 passed** (1 pre-existing). Audit: repository.py accurately recounted to select 5 · insert 1 · bespoke 9 (was drifted at 24/2/0 — the real pre-sweep count was ~37 selects); package totals select 170 → 151, insert 53 → 52, bespoke 86 → 95 (JOIN/subquery/COUNT reclassification). Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05). Chasing the constraint-gap follow-up flagged the day before (`citeulike`/`products`/`standards`/`product_categories` "have no PK/UNIQUE"), found the real story: **those tables don't exist in the reconciled schema at all** — `SELECT sql FROM sqlite_master` returns nothing for them (the earlier `PRAGMA index_list` "PK=NONE" read was a false negative — PRAGMA on a missing table returns empty rather than erroring). The nine methods (`get_citations`/`add_citation`/`delete_citation`, `get_product_categories`/`add_product_category`, `get_products`/`add_product`, `get_standards`/`add_standard`) are **dead code** — zero callers anywhere in mmfdb/chisurf/tests, and any call would raise `no such table`. Deleted them (and their two section-comment headers) rather than leave the just-converted-but-unreachable `dao.insert` calls. The still-present `chem_descriptors`/`images`/`flr_photon_stream`/`optical_properties`/`mmfdb_vocabulary`/`flr_experiment_key_value` tables **do** exist, so those conversions stand. Standalone **421 passed, 1 skipped**. Audit: repository.py select 28 → 24, bespoke 1 → 0 (removed the citeulike `IN(...)` read). Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).

## 2026-07-07
* **MMFDB — eliminated the last `INSERT OR REPLACE` in `repository.py` + `analysis_data`; the package is now `INSERT OR REPLACE`-free (PRD-26).** With `dao.upsert` keyless-tolerant, converted the remaining 11 `INSERT OR REPLACE` writes in the repository core + the `analysis_data` one, choosing the target per table's actual constraint: **real conflict target → `dao.upsert`** — `add_vocabulary` (`field_name,value`), `set_experiment_key_value` (`experiment_id,key`), `add_optical_property` (`probe_id,property_name`), `add_photon_stream` (`stream_id`), `add_analysis_data` (`analysis_id,data_type,data_name`, keeping the load-bearing soft-delete UPDATE that dedups the NULL-`data_name` case SQLite's UNIQUE treats as distinct); **autoincrement-id / no-UNIQUE → `dao.insert`** — `add_chemical_descriptor`, `add_image`, and `add_external_file` (always-fresh uuid, so its follow-up `SELECT id` collapsed into the `insert` return). **A real finding:** `citeulike`, `products`, `standards`, `product_categories` have **neither a PK nor a UNIQUE** in the reconciled schema, so their `INSERT OR REPLACE` was silently a plain insert (duplicate ids possible) — converted to `dao.insert` with a comment making the missing-constraint gap explicit rather than pretending to upsert (a real fix needs a schema PK; noted for follow-up). Standalone **421 passed, 1 skipped**; `test/fio` **236 passed** (1 pre-existing). Audit: insert 64 → 53 (repository.py 13 → 2, its only raw inserts now the append-only `mmfdb_parameter`/`mmfdb_audit_log` core). **No `INSERT OR REPLACE` remains anywhere in the package.** Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — made `dao.upsert` keyless-tolerant; converted the last "keyless-table" `INSERT OR REPLACE` writes (PRD-26).** Root-caused the documented "keyless tables can't be upserted" limitation: `analysis_metadata` and `flr_sample_key_value` have **no PRIMARY KEY but a real `UNIQUE(…, key)`** constraint, so they *are* valid `ON CONFLICT` targets — the only blocker was `dao.upsert` calling `primary_key()` *unconditionally* at the end (to pick a return value), which raised `DaoError` before the caller's explicit `conflict` could help. Wrapped that in `try/except DaoError → return lastrowid`, mirroring the same fix already in `dao.insert`. With that, converted the three surviving `INSERT OR REPLACE` writes onto identity-preserving upserts: `SampleMixin.set_sample_key_value` (`conflict=["sample_id","key"]`) and `AnalysisMixin.add_analysis_metadata` + the `set_analysis_metadata` reseed loop (`conflict=["analysis_id","key"]`) — re-add now clears `deleted_at` in place instead of delete-and-reinsert. Added `test_upsert_into_keyless_table_on_unique` (sibling to the keyless-`insert` test). Standalone suite **421 passed, 1 skipped**. Audit: insert 67 → 64. **`flr_sample_key_value`/`analysis_metadata` are no longer a DAO limitation** — the only genuinely un-upsertable tables left are those with *neither* a PK *nor* a targetable UNIQUE. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — swept the trivial single-table SELECTs in `queries/*.py` onto the DAO (PRD-26).** With the scattered-CRUD migration complete, converted the *trivial* reads in the centralized query mixins — single-table select-by-PK and equality-filter reads → `db.dao.get`/`db.dao.list` — across all 13 query files (users, parameters, experiments, branches, protocols, samples, studies, analysis, setups, probes, artifacts; objects/lifecycle had none convertible). Also folded a handful of coupled raw writes that a SELECT hung off (e.g. `add_experiment_data`'s `INSERT`+`last_insert_rowid()` → `dao.insert`). **Deliberately left raw** (the legitimate centralized home): JOINs, `DISTINCT`/`COUNT`/`MAX` aggregates, `LIKE` search, `json_extract` filters, `IN (…)`/`OR` predicates, `sqlite_master`/`PRAGMA` introspection, subquery/latest-per-group, compound `ORDER BY` (the DAO's `list` is single-column), reads on the external source `spectra.db` connection, and the `consolidate_probes` merge (dynamic `probe_id`/`item_id` key columns). **Lessons that shaped the sweep:** (1) `dao.get` needs an explicit `pk_column` when the dictionary schema doesn't mark the PK and the `<table>_id`/`id`/`uuid` fallback misses (`mmfdb_protocol.protocol_id`, `mmfdb_study.study_id`, `spectra.spectrum_id` — the last has a *separate* autoincrement `id`, so the fallback would silently query the wrong column); (2) `dao.list`/`get` return plain dicts, so any caller doing positional `row[0]`/tuple-unpacking had to be checked first (only `studies.backfill` and a couple of projections kept their raw form for that reason); (3) reads with **no** `deleted_at` guard were preserved with `include_deleted=True`; (4) converting `find_or_add_probe`'s refetch to a dict incidentally fixed a latent `sqlite3.Row.get()` call. Verified after every file — standalone **420 passed, 1 skipped** throughout; `test/fio` **236 passed** (1 pre-existing `ndxplorer` fail). Audit: select **241 → 175**; `probes.py` reads reclassified into bespoke (JOIN/aggregate/source/merge), so bespoke rose 68 → 87 as an accuracy correction, not new raw SQL. This was the optional item-3 cleanup in `HANDOVER.md`; the remaining `queries/` SELECTs are all the sanctioned bespoke home. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — converted the last `INSERT OR REPLACE` in `queries/` (`add_operation_artifact`) to a composite-PK upsert (PRD-26).** The earlier "clear the remaining `INSERT OR REPLACE`/`IGNORE` in `queries/`" pass had left `ArtifactOpsMixin.add_operation_artifact` raw because `mmfdb_operation_artifact`'s 4-column composite PK was believed un-targetable by the DAO. Having disproven that (see the `seed_example` bullet below), routed it onto `db.dao.upsert(conflict=["operation_id","artifact_id","direction","role"])` — the `role`/`direction` are conflict columns and `created_at`/`updated_at`/`deleted_at` are the update set, so on re-add it refreshes the timestamps and **resurrects** a soft-deleted link (`deleted_at → NULL`) in place, identity-preserving where `INSERT OR REPLACE` would delete-and-reinsert. Verified the insert / soft-delete / re-add cycle keeps a single row with `deleted_at=NULL`. The two `INSERT OR IGNORE INTO mmfdb_group_member` writes in `schema.py`'s `bootstrap_*` stay `# raw` — they run on a **bare `sqlite3.Connection`** with no `dao` in scope (the documented bootstrap-seeder pattern). Standalone suite **420 passed, 1 skipped**. Audit: `queries/artifacts.py` insert 6 → 5; total insert 69 → 68. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — routed the demo seeder `admin/seed_example.py` through the DAO (PRD-26 burn-down).** The example-smFRET seeder is the last non-query module carrying scattered CRUD; it holds an `MFDatabase` handle throughout, so `db.dao` is in scope. Converted every existence-check-then-insert helper: the probe/experiment-type/poly-probe-position lookups (`SELECT … WHERE <non-PK/UNIQUE>` → `db.dao.list(filters=…, limit=1)`), the user/device/entity/assembly/condition/artifact/operation existence probes (`SELECT 1 WHERE <pk>` → `db.dao.get(...) is None`), and the raw inserts for `entities` + `entity_poly_seq`, `flr_entity_assembly`, `flr_sample_condition`, and `flr_poly_probe_position` → `db.dao.insert` (the last returning the autoincrement `id` via lastrowid). The `probe_types` `INSERT OR IGNORE`+SELECT-back collapsed into `dao.upsert(conflict=["type_name"])` + `dao.list`. **Correction to a documented "DAO limitation":** the two `INSERT OR REPLACE INTO mmfdb_operation_artifact` writes on the **4-column composite-PK junction** also went onto the DAO — `dao.upsert(conflict=["operation_id","artifact_id","direction","role"])` targets the full composite PK, and since the values *are* exactly those 4 columns the upsert is an idempotent `DO NOTHING` (identity-preserving, unlike the delete-and-reinsert of `INSERT OR REPLACE`). So `seed_example.py` is now **zero raw SQL**; composite-PK junctions are convertible whenever you can name the full PK as the conflict target (only *keyless* tables with no targetable UNIQUE remain a genuine limitation). Verified by **running the seeder twice** against temp DBs (both the flr-tables path and, with fabricated `.spc` fixtures, the artifact/operation/product path) — idempotent, all rows land. Standalone suite **420 passed, 1 skipped**. Audit: `seed_example.py` 13 select · 8 insert → 0/0/0; totals select 254 → 241, insert 77 → 69. **The non-query-module scattered CRUD is now fully eliminated.** Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **OKF — codified the change-tracking loop as a workflow concept.** Added [workflows/change-tracking.md](/workflows/change-tracking.md), the durable statement of the process rule the root `CLAUDE.md` and per-area change policy already point at: for **every material change** → update the matching OKF concept + any burn-down table/DoD, append a dated bullet to [okf/log.md](/log.md), mark done, and commit (locally only — [[feedback-no-push]]). It also spells out *how changes stay traceable* (log = running narrative, `specs/*-audit.md` = running totals, git = small self-describing verified commits, `HANDOVER.md` = the start-fresh brief with an explicit *what's next*). Linked it from [workflows/index.md](/workflows/index.md), pointed the root `CLAUDE.md` "Working practices" and `HANDOVER.md` at it, and folded "then commit" into the CLAUDE.md track-every-edit rule.
* **MMFDB — removed the last scattered reads in `api.py` + `adapters/chinet.py` (PRD-26).** `api.py`'s two `SELECT 1 FROM mmfdb_object_acl …` ACL-existence probes now reuse the already-DAO-backed `security.auth._get_object_acl` (DRY — the ACL lookup lives in one place); `adapters/chinet.py`'s `flr_experiment` existence check → `db.dao.get(..., include_deleted=True)`. Both non-query modules are now SQL-free. (The `provenance/lineage.py` label reads stay raw — that module is bespoke graph traversal: DISTINCT/`IN`/recursive lineage the DAO can't express.) Standalone suite **420 passed, 1 skipped**. Audit: select 257 → 254. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — cleared the remaining `INSERT OR REPLACE`/`INSERT OR IGNORE` writes in `queries/{users,probes,artifacts}.py` (PRD-26).** `UserDeviceMixin.ensure_user` and `add_artifact_owner` (`INSERT OR IGNORE` → existence-check + `dao.insert`, faithful to ignore-on-conflict); `ProbeMixin.add_probe` both branches (plain `INSERT` → `dao.insert`; the dynamic-column `INSERT OR REPLACE` → a built `values` dict + `dao.upsert` keyed on `probe_id`) and its `probe_types` `INSERT OR IGNORE`+SELECT-back collapsed into a `dao.list`/`dao.insert` reuse-or-create; `ArtifactOpsMixin.add_edge`'s `INSERT OR REPLACE INTO mmfdb_edge` (only unique is the autoincrement `edge_id`, never supplied → a plain `dao.insert`) plus the `record_operation` active-branch/head writes (`flr_sample_users.active_branch_uuid`, `mmfdb_branch` create/head-update → `dao.update`/`dao.insert`). Left flagged `# raw`: the probe-merge synonym `INSERT OR IGNORE` (dynamic key column `prop_key`) inside the bespoke dedup routine. Dropped an unused `_utc_now` import in `users.py`. Standalone suite **420 passed, 1 skipped**. Audit: insert 85 → 77, update 50 → 48, bespoke 67 → 68. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — replaced two `INSERT OR REPLACE` writes in `queries/` with identity-preserving upserts (PRD-26).** `ProbeMixin.add_spectrum` (`spectra`, `conflict=["probe_id","spectrum_type"]`) and `SampleMixin.add_sample_probe` (`flr_sample_probe`, `conflict=["sample_id","probe_id","poly_probe_position_id"]`) now use `db.dao.upsert` instead of `INSERT OR REPLACE`. This preserves the row's surrogate `id`/`sample_probe_id` and any FK references on update (the delete-and-reinsert of `INSERT OR REPLACE` rotates the PK and can cascade), while still clearing `deleted_at` on re-add. Verified each table's UNIQUE constraint before choosing the conflict target. Standalone suite **420 passed, 1 skipped**. Audit: insert 87 → 85. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — wired the DAO through the `security/auth.py` boundary (PRD-26 burn-down).** The auth helpers take a bare `sqlite3.Connection` (a transport-agnostic boundary that must not import `MFDatabase`), so added a module-level `_dao(conn)` that builds a `DictionaryDao` on demand with a **process-wide schema cache** keyed by the table-name set — all MMFDB databases share the dictionary-generated schema, so after warmup the per-call cost is one cheap `sqlite_master` read even when `can_access`/`filter_readable` loop over many rows (no repeated full `PRAGMA` introspection). Routed all CRUD-shaped writes and clean single-table reads onto it: ACL creation (`INSERT OR IGNORE` → existence-check + `dao.insert` on the keyless `mmfdb_object_acl`), `grant_acl`/`create_session`/`record_auth_attempt` inserts, `revoke_acl`/session `last_used_at`/`revoke_session[_by_token]` updates (`dao.update`/`soft_delete`), and the `_user_group_ids`/`_check_acls`/`_get_object_acl`/`revoke_acl`-lookup reads. Left genuinely bespoke/flagged `# raw`: the `authenticate_token` session⋈user JOIN, the `is_throttled` `COUNT` aggregates, `chmod`/`chown`/`chgrp` (composite-key updates on `mmfdb_object_acl`'s `UNIQUE(object_type,object_id)` — no single PK for `dao.update`), and `list_sessions` (**deliberate column projection excluding `token_hash`** — the DAO's `list()` has no projection and would leak the token hash). This exercises the keyless-`insert` enhancement (`mmfdb_object_acl`). Standalone suite **420 passed, 1 skipped**. Audit: select 264 → 257, insert 92 → 87, update 57 → 50, bespoke 62 → 67. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — wired the DAO into the three admin service backends + made `dao.insert` keyless-tolerant (PRD-26 burn-down).** Purged the scattered writes from `admin/backend/{password,auth,fluorophore}_services.py`, all of which run under `with MFDatabase(...) as db:` so `db.dao` is in scope. `password_services`: password `UPDATE flr_sample_users` → `db.dao.update`, best-effort audit `INSERT` → `db.add_audit_log`. `auth_services`: `change_password` update, `mmfdb_group` create/dynamic-update/soft-delete, and `members.add`'s `INSERT OR IGNORE` (now a `dao.list(include_deleted=True)` existence-check + `dao.insert`, faithful to the ignore-on-UNIQUE semantics) → DAO; `members.remove` stays raw (`# raw`) as a composite-key soft delete on the PK-less `mmfdb_group_member` the DAO can't target. `fluorophore_services`: the merge routine's `probes` rename → `dao.update` and its three duplicate soft-deletes (`probes`/`optical_properties`/`spectra`) → `dao.soft_delete`, leaving the two cross-row `INSERT … SELECT` bulk copies and the conditional `verification_status != 'approved'` update flagged `# raw`. **DAO enhancement:** `DictionaryDao.insert` no longer requires a resolvable primary key — on a keyless table (UNIQUE-only junction like `mmfdb_group_member`) it returns the last rowid instead of raising `DaoError`, which is what unblocked the `members.add` conversion; covered by a new `test_insert_into_keyless_junction_returns_rowid`. Standalone suite **420 passed, 1 skipped**. Audit: insert 97 → 92, update 66 → 57, bespoke 58 → 62. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — routed the convertible writes in `admin/backend/services.py` through the DAO (PRD-26 burn-down).** The RPC handlers run under `with MFDatabase(...) as db:`, so `db.dao` is in scope. Converted the clean single-table CRUD: the `mmfdb_branch` multi-column `UPDATE … WHERE branch_uuid` → `db.dao.update`; the soft-delete handlers for `flr_sample_condition`, `entities`, `optical_properties` (by `probe_id`), and `flr_poly_probe_position` → `db.dao.soft_delete`; and the `INSERT OR REPLACE INTO flr_sample_condition` → `db.add_sample_condition` (identity-preserving upsert, drops the manual `commit`). The eight writes left are all **intentional-raw**, now flagged `# raw`: the cross-table user-id remap (dynamic table/column rename over FK tables + ACL), two admin **hard deletes** of FRET Förster rows (a soft delete would leave the row occupying the UNIQUE `forster_radius_id` and block re-add), one experiment hard delete, and two **resurrecting** updates on `probes`/`flr_poly_probe_position` (reset `deleted_at = NULL`, which `dao.update` cannot do — it refuses to touch soft-deleted rows). Standalone suite **419 passed, 1 skipped**. Audit: insert 98 → 97, update 75 → 66, delete 15 → 12, bespoke 50 → 58. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — flagged the two `bootstrap_*` seeders as intentional-raw (PRD-26 audit).** `provenance/operation_parameters.py` and `lifecycle/lifecycle.py` seed their def/rule/vocabulary tables from authored JSON on a **bare `sqlite3.Connection`** (no `MFDatabase`/`dao` in scope) via a bespoke bulk-reseed: `INSERT OR IGNORE` vocabulary, delete-by-non-PK per `operation_type`/`entity_type`, `MAX(def_id|rule_id)` reindex, then bulk insert with explicit keys. These are aggregate/bulk-delete shapes the DAO cannot express and are not scattered CRUD, so they stay raw with an explicit `# raw:` marker rather than churning them onto a per-call-introspected DAO. Reclassified in the audit as bespoke (insert 101 → 98, delete 17 → 15, bespoke 45 → 50). Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — converted the two convertible writes in `samples/seed_data.py` (PRD-26 burn-down).** The seed builder already used repository methods for most writes; rerouted the last two hand-SQL writes onto the DAO: the post-`add_probe` `UPDATE probes SET verification_status/quality` → `db.dao.update("probes", probe_id, …)`, and the precomputed-Förster-radius `INSERT OR REPLACE INTO flr_fret_forster_radius` → `db.dao.upsert(..., conflict=["forster_radius_id"])`. The table carries two UNIQUE constraints (`forster_radius_id` and the `(sample_id, donor_probe_id, acceptor_probe_id)` triple); the seed IDs are deterministic so both point at the same row on re-seed, making the single-target upsert faithful — and identity-preserving where `INSERT OR REPLACE` would delete+reinsert (rotating the autoincrement `id`). Dropped the now-unused `_utc_now` import. Standalone suite **419 passed, 1 skipped**. Audit: insert 102 → 101, update 76 → 75. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB — purged scattered CRUD SQL from `samples/sample_manager.py` (PRD-26 burn-down).** Continued the "no scattered raw SQL" migration ([PRD-26](/prds/prd-26.md); tracker [specs/mmfdb-sql-audit.md](/specs/mmfdb-sql-audit.md)) into the non-query modules, where hand-written SQL is the real antipattern. Rerouted every **write** in `sample_manager.py` — the sample-graph builder — off `db.conn.execute` and onto existing repository methods / the dictionary DAO: probe positions → `db.add_poly_probe_position`, sample conditions → `db.add_sample_condition`, PDBx/flrCIF key-values (the keyless `flr_sample_key_value` table) → `db.set_sample_key_value` (whose raw upsert stays centralized in `SampleMixin`), and optical-properties / spectra inserts → `db.dao.insert` with skip-if-present guards via `db.dao.list` (preserving the "don't clobber a reused probe's first values" semantics and the JSON-text spectra serialization that this sample path reads back with `json.loads`, distinct from the numpy-blob `add_spectrum` path). Dropped the now-dead module-local `_utc_now` helper (the DAO/repository methods stamp `created_at`/`updated_at`). The only raw SQL left is reads plus one bespoke 4-column position-existence probe, flagged `# raw`. Standalone suite **419 passed, 1 skipped**. Audit totals: insert 109 → 102. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).

## 2026-07-06
* **MMFDB — god-class breakup: `repository.py` 6,397 → 1,848 lines (−71%).** Resumed and largely completed the Phase-4 [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05) decomposition of the `MFDatabase` god-class into per-concern query mixins under `queries/`. Extracted six new mixins this pass — **ProbeMixin** (probe/spectra/optical/Förster), **SampleMixin** (samples/entities/positions/linkage), **SetupCalibMixin** (setups/calibrations), **ObjectStoreMixin** (object store + materialization), **ArtifactOpsMixin** (the artifact/operation/edge/provenance-graph core, ~46 methods), and **AnalysisMixin** (analysis runs/products/metadata) — for **13 mixins** total. Built a reusable AST-boundary method extractor (handles multi-line signatures, decorators, and leaves class attributes in place); relocated module-level helpers (`_default_reference_spectra_path`→`store/database_resolver`, `_validate_checksum`→`schema/_sqlutil`) to avoid repository↔mixin import cycles, and carried class-level method aliases (`save_setup_snapshot`, `link_operation_artifact`) into their mixins. Public `MFDatabase` surface unchanged (methods resolve via the MRO); standalone suite **419 passed** after every extraction. `repository.py` is now a thin composition over `self.conn`/`self.dao`/`self.lineage` — what remains is the legitimate core (init/connection/properties, migration, audit, vocabulary, experiment key-values, pdbx metadata).
* **MMFDB — admin GUI moved out; `adapters/` subpackage added.** Continued the clean-bounds migration: relocated the chisurf-coupled admin **GUI** (24 modules + 7 view-spec JSONs) out of the package into the ChiSurf plugin (`chisurf/plugins/core/mmfdb_admin/gui/`), replacing the compat wrappers and retargeting 38 importers + the manifest entrypoint; the admin **backend** (RPC handlers) stays in `mmfdb.admin` and is chisurf-free. Added an **`adapters/`** subpackage as the home for external-system bridges — moved `chinet_adapter` → `mmfdb.adapters.chinet` (ChiSurf fit sessions → MMFDB), with eLabFTW / electronic-lab-notebook adapters to follow; adapters keep target-system imports lazy so the core still imports standalone. Top-level `mmfdb` is now 5 files. Standalone: 419 passed. Advances [PRD-24](/prds/prd-24.md)/[PRD-48](/prds/prd-48.md).
* **MMFDB — own hermetic test suite + concern-subpackage reorg (PRD-24).** Gave the package clear bounds and a maintainable file layout. (1) **Relocated ~35 pure-MMFDB test modules** from ChiSurf `test/fio`+`test/plugins` into `modules/mmfdb/tests/` with a hermetic conftest (isolation via MMFDB's own `MMFDB_SETTINGS_DIR`, no chisurf import); the suite runs standalone (`cd modules/mmfdb && PYTHONPATH=src pytest`). This immediately exposed **real boundary violations** in the package — fixed the module-level ones (`admin/backend/services` used `from chisurf import logging` → stdlib; `{measurement,ndxplorer}_services` imported `chisurf.server.services` error helpers → new `mmfdb.admin.backend._service_errors`). ChiSurf↔MMFDB integration tests (admin GUI `MMFDBClient`, chinet/`parameter_registry` alignment, ndxplorer submodule, chisurf test-data) stay in the ChiSurf tree. (2) **Reorganized the 38 flat top-level modules into concern subpackages** — `schema/ store/ provenance/ samples/ lifecycle/ security/ project/` — leaving 6 core files at top (`repository`, `api`, `models`, `config`, `chinet_adapter`, `__init__`); collapsed the `auth/`-only-`__init__` "package" to `auth.py`. Hard cutover (no compat shims): every internal, test, and ChiSurf import retargeted to grouped paths (`mmfdb.schema.dao`, `mmfdb.provenance.result_registry`, …); fixed `Path(__file__).parent/"data"` resolution in the seven moved modules that read bundled data. **Proof of clean bounds: 419 passed with only `src` on the path** (no chisurf, no chinet). ChiSurf `test/fio`: 236 passed, 1 pre-existing failure (`test_ndxplorer_cli`, needs the ndxplorer submodule). Advances [PRD-24](/prds/prd-24.md); layout documented in [architecture/mmfdb.md](/architecture/mmfdb.md).
* **MMFDB — reimplemented external-reference persistence (PRD-39); test/fio 7 → 1.** The struct_ref/external-reference write path was lost when the SQLAlchemy `orm/` stack was deleted (Merge 1) and never reproduced on the raw-SQL path — `create_sample` recorded entities but silently dropped their UniProt/PDB cross-references and mutations. Reimplemented minimally: `sample_manager._persist_entity_external_refs` records `uniprot_accession`→`struct_ref(db_name=UNP)`, `pdb_id`→`struct_ref(db_name=PDB)`, the primary reference's sequence alignment as `struct_ref_seq`, and engineered mutations (given, or auto-diffed from `reference_sequence` via `external_refs.diff_sequences`) as `struct_ref_seq_dif`; `get_sample_full_description` now surfaces per-entity `external_refs`/`mutations`, and the probe↔mutation export validator resolves the entity via the position's `entity_id` (added to the read) with a `residue_number`/`residue_name` fallback for `seq_id`/`comp_id`. **Writes go through the dictionary-driven `DictionaryDao` (`db.dao.insert`/`list`), not hand SQL** — and codified that: raw/f-string SQL for CRUD is now documented as an **antipattern** in [architecture/mmfdb.md](/architecture/mmfdb.md) (one engine = `db.dao`; raw SQL only for bespoke joins/graph/aggregate reads). Also fixed a latent `_array_to_text` `TypeError` (np.frombuffer on a text blob) in the flrCIF export path. `test/fio` now **656 passed / 1 failed**; the lone remaining failure (`test_ndxplorer_cli`) is an out-of-scope environment issue (needs the `ndxplorer` submodule built). Advances [PRD-19](/prds/prd-19.md)/[PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MMFDB simplification — post-cutover stabilization: test/fio 179 → 7 failures.** The Merge-3 cutover exposed a broken full suite; root-caused and fixed it in committed batches. **The dominant cause (~150 cascading failures) was a single global-state leak:** `test_fcs_setup.py`'s v36-migration test set the module global `schema.SCHEMA_VERSION = 35` and never restored it, so every later test's fresh `MFDatabase` skipped the version-40 reconcile-to-`.dic` step and came up with a partial schema (missing `mmfdb_state_transition`, `mmfdb_operation.protocol_id`). Removed that obsolete test (no v36 migration exists after the PRD-19 collapse to `MIGRATIONS={1,40}`). Then fixed the independent failures: a **live default-user-id bridge** ([PRD-24](/prds/prd-24.md)) — `mmfdb.config.set_default_user_id_resolver` consulted by `configured_default_user_id`, registered from `chisurf.core.settings` to read `cs_settings` on each call (MMFDB stays standalone); a real **`get_parameter` bug** (delegated to `dao.get` on the table PK `parameter_id` instead of the `parameter_uuid` column → always None); `importer.py` `get_probe_by_id`→`get_probe`; `migrate_schema` returns no report for a fresh (from-scratch) database; and a batch of stale-reference test fixes after the cutover (flr_ext `.dic` via `MmcifDictionary.DATA_DIR`, `python -m mmfdb.pdbx_metadata`, `mmfdb.admin.backend.services`, admin session-token minting now that a fresh DB seeds a `user_default` admin, stale `get_optical_properties`/`get_spectrum` shapes, explicit plugin-manifest path). **Remaining 7:** `test_external_refs` (6) is a real **PRD-39 feature reimplementation** — struct_ref/external-reference persistence (uniprot/pdb→`struct_ref`, mutations→`struct_ref_seq_dif`, sequence auto-diff, export validation, and surfacing `external_refs`/`mutations` in `get_sample_full_description`) was lost when the SQLAlchemy `orm/` stack was deleted in Merge 1 and never reimplemented on the raw-SQL path; `test_ndxplorer_cli` (1) is an out-of-scope environment issue (needs the `ndxplorer` submodule built). Advances [INC-05](/specs/assessment.md#inc-05)/[PRD-24](/prds/prd-24.md).
* **MMFDB simplification — Merge 3 cutover landed: one import namespace, dead shim gone.** Committed the previously-uncommitted cutover in three coherent local commits. (1) Deleted the shadowed `chisurf/core/mmfdb/*.py` shim modules — the facade `__init__.py` already routes `chisurf.core.mmfdb.X → mmfdb.X` via its meta-path `_AliasFinder`, so the sibling real files were duplicate shadows whose presence broke test isolation (two `database_resolver`/event-bus copies); kept the thin facade + `auth/` + `data/`. (2) Repointed the `mmfdb_admin` plugin at the relocated `mmfdb.admin.*` package (manifest entrypoints + backend/GUI compat wrappers) and deleted the retired `sample_database` plugin. (3) Fixed a bad find-replace from the cutover that had rewritten five test modules' admin Python import paths `mmfdb_admin → mmfdb.admin`, producing the nonexistent hybrid `chisurf.plugins.core.mmfdb.admin.*` and a hard `ModuleNotFoundError` (the "3 stale reds" flagged in the Merge-1 note, actually 5) — repointed to canonical `mmfdb.admin.*`, unblocking 136 tests. Net effect: clean-HEAD `test/fio` went from **660 collection errors** (duplicate-module chaos) to a runnable suite; mmfdb-core gate still 161 passed. **Newly exposed** by the now-running suite: ~163 `test/fio` failures across three root causes — global-state test-ordering leaks, `cs_settings`-not-flowing-into-standalone-`mmfdb.config` decoupling (PRD-24, deferred), and a stale hardcoded `CREATE_TABLES_SQL` (`mmfdb_operation` missing `protocol_id`/`protocol_version` vs the canonical `_TableDef`, Phase 3). Stabilization is the next step. Advances [INC-05](/specs/assessment.md#inc-05)/[PRD-24](/prds/prd-24.md).
* **MMFDB simplification — Merge 2 in progress: god-class breakup begun.** Added identity-preserving `DictionaryDao.upsert` (INSERT…ON CONFLICT DO UPDATE — the safe write path the `add_*`/`save_*` family re-roots onto). Extracted pure JSON/time helpers into `mmfdb/_sqlutil.py` (breaks the import cycle mixins would form). Splitting the 7,322-line `MFDatabase` god-class into per-concern mixins under `mmfdb/queries/`: **ParameterMixin, StudyMixin, ProtocolMixin, LifecycleMixin, BranchMixin, UserDeviceMixin, ExperimentMixin** (7 concerns; public method surface unchanged — methods resolve via MRO, zero delegators); `repository.py` 7,322 → 6,389 lines. Shared pure helpers (`_row_to_dict`, `_exists` too) live in `_sqlutil`. Extracting LifecycleMixin exposed and fixed a latent bug in the `chisurf.core.mmfdb` compatibility facade — it set `__path__` to the vendored package, so a submodule imported through the facade before `mmfdb.X` loaded became a **duplicate module** (two event buses); a meta-path finder now aliases every `chisurf.core.mmfdb.X` to the canonical `mmfdb.X`, fulfilling the facade's "no duplicate class objects" contract. mmfdb-core gate green throughout (now 161 passed, incl. test_lifecycle/test_events). Remaining: the tightly-coupled provenance **core** (probes/spectra/forster, samples, artifacts/operations/objects/edges, provenance-graph, setups/calibrations, entities, analysis-metadata) — more entangled, best extracted carefully — then Phase 3 (fully `.dic`-generated schema) and Merge 3 (import unification, ABC drop). Advances [INC-05](/specs/assessment.md#inc-05)/[PRD-26](/prds/prd-26.md).
* **MMFDB simplification — Merge 1 (of 3) landed: one fewer access layer.** Started the MMFDB de-monolith program (finishing [PRD-19](/prds/prd-19.md)/[PRD-26](/prds/prd-26.md)/[PRD-24](/prds/prd-24.md); target [specs/mmfdb.md](/specs/mmfdb.md); backlog [INC-05](/specs/assessment.md#inc-05)). (1) Tracked the previously-untracked `modules/mmfdb/` tree (source + `.dic`/`spectra.db` data assets) in git so the package is versioned. (2) Added a public API for the private helpers callers reached into — `utc_now`, `json_loads`, `get_active_database`, `session_token_registry`, `hash_token` (private names kept as-is; purely additive). (3) **Deleted the SQLAlchemy `orm/` layer** (`orm/{base,models,sample_repository,sync}.py` + `test_orm.py`, ~2,700 lines): it duplicated the raw-SQL repository over the same tables and existed mainly to be kept in sync with it. Its one production consumer (`get_sample_full_description`'s ORM branch) already had a complete raw-SQL fallback, now the sole path; the three parallel access styles are now two. Remaining (Merge 2/3): standardise on the dictionary-driven `DictionaryDao` engine, add `DictionaryDao.upsert`, split the 7,322-line `MFDatabase` god-class into per-concern mixins, make the schema fully `.dic`-generated, unify the three import paths, drop the `base.py` ABC. mmfdb-core test gate green throughout (103→118 passed). Pre-existing reds noted: 3 test files carry stale `chisurf.plugins.core.mmfdb.admin.*` imports (→ fix in the import-unification merge).
* **PRDs fully migrated into OKF; `overhaul/` retired.** The `okf/prds/` group is now the **self-contained** home for every PRD: all 61 concepts were expanded from thin summaries to carry the full design text folded from their `overhaul/` sources (and companion docs — PRD-02 summary/completion/review, PRD-03/04 code reviews & punch-lists, PRD-10 bug note, PRD-31 pyqtgraph-migration), with curated frontmatter/status preserved (OKF wins on conflict), `# Source` sections dropped, `resource:` repointed at code, external products kept role-based, and no `overhaul/` links. Added two new PRDs from the retired working notes — [PRD-57 (ChiMOL command parity + renderer migration)](/prds/prd-57.md) and [PRD-58 (FRET FPS+OLGA superset)](/prds/prd-58.md) — plus the authoritative [PRD Implementation Order](/prds/master-order.md). Durable non-PRD `overhaul/` docs were rehomed as references: [MMFDB architecture ideas](/references/mmfdb-architecture-ideas.md), [MMFDB LIMS diagnosis](/references/mmfdb-lims-diagnosis.md), [node/workflow-toolkit lessons](/references/orange3-lessons.md), [modelling/ProteinMC roadmap](/references/modelling-roadmap.md); the MMFDB dictionary programming rules folded into [architecture/mmfdb.md](/architecture/mmfdb.md), IDEA-01 (structure-aware sample definer) into [modelling](/plugins/modelling.md), and the background-estimation upgrade into [burst](/plugins/burst.md). Updated the prds/references/root indexes, `overview.md`, and `roadmap-prds.md` to drop `overhaul/` pointers, then deleted the `overhaul/` folder. Ephemeral `overhaul/` files (README/START_HERE/TODO/completion reports, superseded plans) were dropped as archive.
* **CLAUDE.md refresh + AGENTS.md folded in.** Added a "Working practices" policy — *mark work done in its tracker* (PRD `status:`/DoD, index glyph, assessment row) and *track every edit in OKF* (update the matching concept + append to this log). Folded the durable parts of the removed root `AGENTS.md` into CLAUDE.md (change policy, Qt/`--no-deps`/`python -m chisurf`/`PYTHONPATH` gotchas, docs build, testing & rendering conventions, imp-tricks split pointer, client-server plugin standard, object-store pointer to [architecture/mmfdb.md](/architecture/mmfdb.md)); stale bits were redirected to OKF rather than copied, and `overhaul/` references in CLAUDE.md were repointed at `okf/prds/`.
* **Notes integration**: Folded the loose top-level `notes/` working files into the bundle so their durable knowledge is curated rather than lost. New concepts: [ChiMOL viewer profile](/plugins/profiles/chimol.md) (PyMOL-style `cmd` surface + Qt-free renderer / ImGui-migration constraints, from the moview→chimol cmd-parity notes) and [Known issues & gotchas](/references/known-issues.md) (still-open functional bugs + recurring engineering pitfalls distilled from two working bug logs). Enriched existing concepts: [MMFDB](/architecture/mmfdb.md) gained an object-store & provenance-aware-reader section; [MMFDB Admin](/plugins/profiles/mmfdb-admin.md) gained the dictionary-driven `EntityDock` architecture; [Modelling](/plugins/modelling.md) gained durable FRET-engine facts (AV backends, `fps.json` keys, evaluators, OLGA reuse) + the superset roadmap pointer; [GUI & AutoForm](/subsystems/gui-autoform.md) gained the shared node-editor widget (versioned JSON graph schema + DAG); [Light Path Simulator](/plugins/profiles/lightpath-simulator.md) gained a preset-format / graph-normalization / macOS-combobox gotchas section. Verified against the tree first — the object store, mmfdb-admin `EntityDock`, chimol `sele_parser`, and FRET evaluators have all landed; the node editor now lives at `chisurf/gui/widgets/node_editor`. Fixed-bug play-by-play and already-resolved TODOs were treated as archive and not copied.
* **MMFDB Admin packaging**: Moved the MMFDB Admin application into the MMFDB package as `mmfdb.admin` (backend, GUI, CLI, and JSON view specs). The ChiSurf `core/mmfdb_admin` plugin now points its manifest and wrappers at `mmfdb.admin`, and root package discovery no longer includes `modules/mmfdb_admin/src` as a separate package source.
* **MMFDB dictionary relocation**: Confirmed MMFDB dictionary files (`*.dic`) live under `modules/mmfdb/src/mmfdb/data/`; the old `chisurf/core/mmfdb/data` dictionary copy has been removed.
* **MMFDB extraction**: Removed the ChiSurf `DataCurve` import from `mmfdb.payload_models`; `GenericCurve` now serializes curve-like arrays and returns plain curve kwargs. Added boundary coverage so `payload_models`, `result_registry`, runtime config, and session/database resolution stay free of ChiSurf imports.
* **MMFDB seed data**: Removed the ChiSurf fluorescence helper imports from `mmfdb.seed_data`; seed Förster overlap/radius calculations are now local NumPy helpers with boundary coverage. Bundled the fluorophore reference `spectra.db` under `modules/mmfdb/src/mmfdb/data/` so seeded/reference imports no longer resolve from the ChiSurf `_dev` plugin path by default.
* **MMFDB design issue**: Replaced `MFDatabase.add_probe_type()` `INSERT OR REPLACE` with a non-destructive upsert so reseeding/updating probe types preserves primary keys referenced by imported probes under foreign-key enforcement.
* **MMFDB seed data API cleanup**: Updated curated seed-data entity creation to the current `MFDatabase.add_entity(entity_id, name, entity_type, details)` API instead of stale keyword arguments.
* **MMFDB seed data API cleanup**: Updated curated seed-data sample-probe creation to use explicit `fluorophore_type`, `description`, and `poly_probe_position_id` keyword arguments instead of stale positional ordering.
* **MMFDB repository API cleanup**: Extended `get_sample_probe_mappings()` to support all-mapping queries and `probe_id` filters, matching existing seed/admin/importer call shapes.
* **MMFDB project archiver extraction**: Removed the ChiSurf experiment serialization import from `mmfdb.project_archiver`; project curve arrays are encoded locally as dtype/shape/base64 dictionaries and covered by the MMFDB import-boundary test.
* **MMFDB sample manager design cleanup**: Updated tests and fallback behavior for the prerelease hard cutover: structured sample creation uses the SQLite graph adapter by default, SQLAlchemy remains optional, and raw-SQL full-description reads include a stable nullable `condition` object plus display-name-backed descriptions.
* **MMFDB facade**: Aliased loaded `mmfdb.*` submodules under `chisurf.core.mmfdb.*` so transitional imports do not duplicate class objects. Added regression coverage for base, repository, payload codec, and payload models module identity.
* **MMFDB Admin evidence**: Updated the current status to reflect green arm64 Qt verification: MMFDB Admin plugin-local suite at 103 passed / 2 warnings and the adjacent MMFDB Admin + Database Connector + Project Browser smoke at 117 passed / 2 warnings. GUI construction-only surfaces remain marked as not behavior-done in the coverage ledger.
* **MMFDB Admin path contract**: Verified compile/import and focused tests with `PYTHONPATH="modules/mmfdb/src:modules/chinet:modules/imp-tricks/src:."` and no `modules/mmfdb_admin/src`. The integration/startup slice reported 60 passed, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 117 passed / 2 warnings.
* **MMFDB Admin Measurements GUI**: Added sample-data-backed headless coverage for the Measurements aggregate panel. The test drives `MMFDBWidget` in ChiSurf context, refreshes seeded raw/processing/processed rows, and verifies kind/search filtering. Fixed the client routing so generic processing and processed-product lists use `mmfdb.processing.list` / `mmfdb.processed_data.list`, added the `processed_data_id` alias to generic product rows, made operation listing status-aware, and removed stale undefined `_exc` logging paths from `mmfdb.admin.gui.tool`. The plugin-local suite reported 105 passed / 2 warnings, the focused GUI/admin/database smoke reported 45 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 119 passed / 2 warnings.
* **MMFDB Admin Provenance Graph GUI**: Added sample-data-backed headless coverage for loading a raw -> processing -> processed provenance graph through `MMFDBWidget`. Fixed the shared test DB patch so split backend service modules (`measurement_services`, `ndxplorer_services`, `fluorophore_services`) resolve to the temporary MMFDB, and canonicalized provenance graph seed node types so `processed_data` seeds do not create duplicate `processed_data:*` and `artifact:*` nodes. The plugin-local suite reported 107 passed / 2 warnings, the focused GUI/admin/database smoke reported 46 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 121 passed / 2 warnings.
* **MMFDB Admin Import / Export GUI**: Added sample-data-backed headless coverage for the Import / Export panel: export validation, flrCIF preview, validation-warning confirmation, sample CIF export, sample-table CSV export, and minimal CIF file import through the GUI file field. Kept interaction-test widgets alive for the module to avoid Qt queued-callback crashes after C++ widget deletion. The plugin-local suite reported 108 passed / 2 warnings, the focused GUI/admin/database smoke reported 47 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 122 passed / 2 warnings.
* **Database Connector side effects**: Added temporary-file coverage for service-only `database_connector` backup, reset-from-source, minimal CIF import, and FLR CIF text/file export. The connector-local suite reported 4 passed; the focused GUI/admin/database smoke reported 48 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 123 passed / 2 warnings.
* **Project Browser deeper coverage**: Added sample-data-backed Project Browser tests for branch DAG roots/leaves, branch version counts, artifact browsing including project-contained fit results, fit-parameter listing, import collision rejection/remapping, delete manage-permission enforcement, and headless GUI delete/import actions. Fixed version graph root/leaf semantics for stored `newer -> parent` supersedes edges, made artifact browsing include `project_contains` artifacts, and repaired `.csp` import collision handling so object UUIDs are remapped through the deduplicating MMFDB object store while preserving valid artifact storage vocabularies. The Project Browser plugin-local suite reported 10 passed, the focused GUI/admin/database smoke reported 54 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 129 passed / 2 warnings.
* **Project Browser restore GUI**: Added headless GUI coverage for restoring a selected MMFDB project version into a patched ChiSurf context. The test uses temporary sample data, the real in-process Project Browser services, verifies `load_project_payload()` receives the restored `Project`, and checks active ChiSurf project metadata updates. The Project Browser plugin-local suite reported 11 passed / 1 warning, the focused GUI/admin/database smoke reported 55 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 130 passed / 2 warnings.
* **MMFDB Admin Objects GUI**: Moved object-store Copy UUID, Reveal, and Delete object actions onto the visible Objects EntityDock and added sample-data-backed headless coverage for selecting the seeded object row, copying its UUID, revealing the object-store blob path, confirming deletion, and verifying MMFDB object removal. The MMFDB Admin plugin-local suite reported 109 passed / 2 warnings, the focused GUI/admin/database smoke reported 56 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 131 passed / 2 warnings.
* **MMFDB Admin data-action GUI**: Moved raw-data, processed-product, and analysis read-only actions onto the visible EntityDock path and added sample-data-backed headless coverage for Copy ID, Reveal/Open where file-backed, and provenance-seed selection. The seeded raw and processed artifacts now use real temporary files so the open/reveal path is exercised against concrete local paths. The MMFDB Admin plugin-local suite reported 110 passed / 2 warnings, the focused GUI/admin/database smoke reported 57 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 132 passed / 2 warnings.
* **MMFDB Admin analysis drilldown GUI**: Added `mmfdb.analysis.full` as a manifest-declared read-only RPC backed by `MFDatabase.get_analysis_run_full()`, added a visible Analysis Details action, and covered the non-modal parameter/output-product drilldown with seeded analysis data. Remaining analysis GUI gap is now non-empty input-product/grouped-fit drilldowns. The MMFDB Admin plugin-local suite reported 110 passed / 2 warnings, the focused GUI/admin/database smoke reported 57 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 132 passed / 2 warnings.
* **MMFDB Admin artifact validation/delete GUI**: Added repository-level artifact validation and soft-delete operations, exposed them as manifest-declared `mmfdb.artifacts.validation.set` and `mmfdb.artifacts.delete` RPCs, and moved validation/delete controls onto the visible Raw Data and Processed Products EntityDocks. Added sample-data-backed headless coverage for validation status changes, confirmed soft delete, table refresh, and operation-link soft-delete for seeded raw and processed artifacts. The MMFDB Admin plugin-local suite reported 111 passed / 2 warnings, the focused GUI/admin/database smoke reported 58 passed / 2 warnings, and the broad MMFDB Admin + Database Connector + Project Browser smoke reported 133 passed / 2 warnings.

## 2026-07-05
* **SV-03 fix**: Routed `chisurf.*` globals through `ChiSurfAPI` `SessionState` — API owns `self._state` aliasing the global lists, all API methods use `self._state.fits`/`self._state.datasets` instead of direct `cs.*` access. 84 tests pass. 12 total fixed.
* **DATA-03 fix**: Consolidated duplicated `mmfdb_*` DDL into single-source `_TableDef`/`_Column` dataclasses — both permissive (migration) and CHECK-constrained (fresh-DB) DDL are generated from `_CANONICAL_TABLE_DEFS`. Removed hand-written `_CANONICAL_CHECK_SQL` and fragile string-matching `_CANONICAL_TABLE_MAP`. 2 guardrail tests. 11 total fixed.
* **DATA-02 fix**: Added ordered migration waterfall to MMFDB schema versioning — `MIGRATIONS` `OrderedDict` (v1 fresh DB, v40 reconcile), `migrate_schema()` reads stored version and applies pending steps sequentially. 6 tests. 10 total fixed.
* **SV-04 fix**: Promoted service errors to the JSON-RPC `error` member — `ZmqServer._handle_one()` converts `ok: False` handler results to proper JSON-RPC error responses. `ChisurfClient.call()` now raises `RemoteError` for both application and transport errors. 14 tests updated. 9 total fixed.
* **S2 fixes**: Landed 3 S2 items — SV-02 (deleted dead DTO dataclasses), SV-05 (event topic contract in `server_methods.json` + guardrail), BUG-04 (`Model` ABCMeta enforcement). Updated assessment statuses.
* **S1 fixes**: Landed all 5 S1 items from the [assessment backlog](/specs/assessment.md) — BUG-01 (NCurve `elif` guard), BUG-02 (stale `fit_select` removed), BUG-03 (`component_type` NameError), SV-01 (Qt-free `chisurf/core/data_io/detector_setups.py`), DATA-01 (3 broken manifests + validation logging). Each committed separately with tests. Added guardrail tests: `test_no_duplicate_handler_names` (BUG-02), `test_no_gui_import_in_server` (SV-01), `test_builtin_manifests_all_valid` (DATA-01). Updated assessment statuses. Created `chisurf/core/data_io/` package.
* **Population**: Expanded OKF coverage from the code tree: added subsystem concepts for [fluorescence domain](/subsystems/fluorescence-domain.md), [project persistence](/subsystems/project-persistence.md), and [pipelines](/subsystems/pipeline.md); added plugin-group concepts for [TTTR tools](/plugins/tttr.md), [trajectory tools](/plugins/trajectory.md), and [sample/spectra/curation](/plugins/sample-spectra.md). Updated subsystem/plugin indexes and cross-links so existing populated pages are discoverable.
* **Integration**: Added the [prds](/prds/index.md) group — one OKF concept per PRD (persistent `PRD-NN` number as stable id, status + phase frontmatter) integrated from the `overhaul/` folder, which is kept intact as the authoritative full text (each concept's `resource` points back). References to external/third-party software are described by role, not named. Index grouped by implementation phase.
* **Migration**: Moved the former top-level `SPECS/` target specs into the OKF bundle as the [specs](/specs/index.md) group — `overview`, `core`, `rpc`, `mmfdb`, `plugins`, the [assessment](/specs/assessment.md) backlog, and the [template](/specs/template.md). Converted each to an OKF concept (frontmatter + bundle-relative links) and cross-linked the target specs to their current-state concepts. Removed `SPECS/`.
* **Initialization**: Created the ChiSurf OKF bundle — root [index](/index.md), [overview](/overview.md), and the `architecture/`, `subsystems/`, `workflows/`, and `references/` groups.
