"""Model-side plot support: data the model owns, a plot displays.

GUI-free definitions for plot behaviour that belongs to the model layer --
currently the editable plot-only reference-transform parameters that a
lineplot exposes and a lifetime model may declare.
"""
