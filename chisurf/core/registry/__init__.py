"""Machine-readable registries: one lookup point per vocabulary.

Registries of things other subsystems enumerate rather than hard-code: the
tttrlib routine/experiment registry (with its AutoForm model), the parameter
groups that live outside ``chisurf.fits`` (plugins, ndxplorer), and the
measurement file formats (``file_formats.json`` ships beside the module that
reads it).
"""
