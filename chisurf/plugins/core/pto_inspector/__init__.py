"""Look inside a ``.pto`` photon container: what is in it, and how it got there.

See :mod:`chisurf.plugins.core.pto_inspector.core` for the Qt-free reading — the
artifact list, the provenance DAG as a node-editor graph, and the payloads — and
:mod:`chisurf.plugins.core.pto_inspector.gui.tool` for the window that draws it.
The headless equivalent is ``csg_pto_inspect``.
"""
