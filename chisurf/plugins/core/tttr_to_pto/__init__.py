"""Two-way conversion between a vendor photon file and its `.pto` container.

See :mod:`chisurf.plugins.core.tttr_to_pto.api` for the Qt-free ``convert``
(pack) / ``extract`` (unpack) calls, :mod:`chisurf.plugins.core.tttr_to_pto.gui.guard`
for the drop-guard (:mod:`chisurf.gui.widgets.dropguard`) that offers packing
when a vendor file is dropped onto a drop zone that opted in, and
:mod:`chisurf.plugins.core.tttr_to_pto.gui.tool` for the bare drop target that
does either direction with no prompt.
"""
