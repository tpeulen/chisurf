"""Burst- and state-wise MLE lifetime fitting of an H2MM analysis.

A post-H2MM step: where the burst-wise MLE fits one decay per burst, this fits
one per (burst, state, colour), which is the unit a burst with dynamics actually
has. See :mod:`.core.state_mle`.
"""
