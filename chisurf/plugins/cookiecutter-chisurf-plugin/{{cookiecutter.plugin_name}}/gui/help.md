# {{ cookiecutter.plugin_display_name }}

{{ cookiecutter.plugin_description }}

> Replace this file with real help. It is what the `?` button shows, and it is
> checked by `test/test_plugin_help_guide_seam.py` — it must clear a minimum
> length and every relative link must resolve to a file that exists.

## What this measures

Say what the quantity *is* and what it is a measurement **of**, before saying how
to compute it. If the tool fits something, say what is being fitted to what; if
it reads something off directly, say that instead, because the two are believed
differently.

## The settings that decide the answer

Not all of them — the two or three that actually move the result. For each, say
what goes wrong when it is wrong, and whether the failure is *visible* or
*silent*. A silent one deserves the most words: it is the one that costs someone
a month.

## Before you believe the result

A short numbered list of checks, in the order that catches the most. Prefer
checks with a known answer — a control sample, a limiting case, a second method
that shares no code with the first.

## Further reading

Write links, not paths, so the modal can follow them: a documentation page opens
in the ChiSurf documentation browser, a URL or DOI in the system browser.

- [Some concept](docs/concepts/index.rst)
