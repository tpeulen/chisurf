{
  "_comment": "Declarative AutoForm layout for the Rotate/Translate-Trajectory tool. A single panel with the custom IO section (trajectory picker + 3x3 rotation-matrix grid + 3-field translation row + save button, H5 browse/drag-drop), the read stride, above a live log rendered by the built-in `info` section bound to the view-model's `log_html`. Replaces the former rotate_translate_traj.ui.",
  "sections": [
    {
      "type": "panel",
      "title": "Rotate / translate trajectory",
      "n_col": 1,
      "sections": [
        {
          "type": "custom",
          "key": "traj_rotate_translate_io"
        },
        {
          "type": "value",
          "attr": "stride",
          "label": "Stride",
          "kind": "int",
          "minimum": 1,
          "maximum": 999999,
          "description": "Read every Nth frame of the source trajectory."
        },
        {
          "type": "info",
          "source": "log_html",
          "height": 100
        }
      ]
    }
  ]
}
