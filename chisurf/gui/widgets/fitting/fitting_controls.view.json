{
  "_comment": "Fit controller: one foldable box, as compact as the hand-drawn fittingWidget.ui it replaces. The actions come first; the fields left inline are the ones that belong to *this* fit (which dataset, which range, which result). Everything that configures a run -- sampler, steps, runs, thinning, chunking, chi2 cutoff, chain format, optimiser tolerances -- lives behind the settings button and is written to the user settings, because it is shared by every fit.",
  "sections": [
    {
      "type": "panel",
      "title": "Fit",
      "collapsible": true,
      "collapsed": false,
      "n_col": 2,
      "sections": [
        {
          "type": "button_row",
          "buttons": [
            {
              "label": "▶  Fit",
              "action": "fit",
              "description": "Run the optimiser on the selected fit."
            },
            {
              "label": "Sample",
              "action": "sample",
              "description": "Sample the posterior of the free parameters. Writes the chains, the diagnostics and the project to a folder."
            },
            {
              "label": "⚙",
              "action": "settings",
              "description": "Sampling and fitting settings: which sampler, thinning, chunk size, χ² cutoff, chain format and the optimiser tolerances. Saved to your settings."
            },
            {
              "label": "auto",
              "action": "auto_range",
              "description": "Choose the fit range from the data: the rising edge to where the signal reaches the background."
            },
            {
              "label": "…",
              "action": "select_dataset",
              "description": "Attach a different dataset to this fit."
            }
          ]
        },
        {
          "type": "choice",
          "attr": "dataset_index",
          "label": "Dataset",
          "kind": "combo",
          "options_source": "dataset_options",
          "description": "Which fit of this window the controls act on."
        },
        {
          "type": "value",
          "attr": "result_index",
          "kind": "int",
          "label": "Result",
          "minimum": 1,
          "description": "Which stored result of this fit is shown."
        },
        {
          "type": "value",
          "attr": "xmin",
          "kind": "int",
          "label": "First",
          "minimum": 0,
          "description": "First channel included in the fit."
        },
        {
          "type": "value",
          "attr": "xmax",
          "kind": "int",
          "label": "Last",
          "minimum": 0,
          "description": "Last channel included in the fit."
        },
        {
          "type": "value",
          "attr": "xmin2",
          "kind": "int",
          "label": "First (2)",
          "minimum": 0,
          "description": "First channel of the second axis, for two-dimensional data."
        },
        {
          "type": "value",
          "attr": "xmax2",
          "kind": "int",
          "label": "Last (2)",
          "minimum": 0,
          "description": "Last channel of the second axis, for two-dimensional data."
        },
        {
          "type": "toggle",
          "attr": "local_first",
          "label": "Local first",
          "description": "Optimise each dataset on its own before the global fit, which usually starts the global step much closer to the optimum."
        }
      ]
    }
  ]
}
