{
  "sections": [
    {
      "type": "panel",
      "title": "Slow-storage staging",
      "collapsed": false,
      "description": "Large data files (e.g. TTTR) on slow or network storage are copied to a fast local cache first, behind a progress bar that shows transfer speed, so the interface does not freeze while reading.",
      "sections": [
        {
          "type": "toggle",
          "attr": "enabled",
          "label": "Enable staging",
          "description": "Copy slow/network files to a local cache before reading. Turn off to always read files in place."
        },
        {
          "type": "value",
          "attr": "threshold_mbps",
          "label": "Slow below",
          "kind": "float",
          "decimals": 0,
          "minimum": 1.0,
          "maximum": 100000.0,
          "step": 10.0,
          "suffix": " MB/s",
          "description": "Sources measured slower than this during a short head-read probe are staged locally. Local SSDs read at hundreds of MB/s; network shares are often much slower."
        },
        {
          "type": "value",
          "attr": "min_size_mb",
          "label": "Min file size",
          "kind": "float",
          "decimals": 0,
          "minimum": 0.0,
          "maximum": 1000000.0,
          "step": 8.0,
          "suffix": " MB",
          "description": "Files smaller than this are always read in place — staging tiny files is not worth the overhead."
        }
      ]
    },
    {
      "type": "panel",
      "title": "Plotting",
      "collapsed": false,
      "description": "A raw per-photon plot (a time trace, a delta-time diagnostic) is thinned to this many points, keeping each bin's minimum and maximum so bursts and dips survive. The budget is for the whole plot and is split across the curves in it, so drawing an all-photon and a selected-photon layer does not spend it twice. An already-aggregated plot (a histogram, a correlation curve) is never touched.",
      "sections": [
        {
          "type": "value",
          "attr": "max_plot_points",
          "label": "Max points per plot",
          "kind": "int",
          "minimum": 10000,
          "maximum": 50000000,
          "step": 10000,
          "description": "Points one plot may draw in total. The default of 150000 is already about 100 samples per screen pixel, so raising it costs repaint time without changing what you see; lower it if a large measurement's plots still lag."
        }
      ]
    },
    {
      "type": "panel",
      "title": "Advanced",
      "collapsed": true,
      "description": "Fine-tuning for the probe and copy loop. The defaults are appropriate for most setups.",
      "sections": [
        {
          "type": "value",
          "attr": "chunk_mb",
          "label": "Chunk size",
          "kind": "float",
          "decimals": 2,
          "minimum": 0.25,
          "maximum": 256.0,
          "step": 1.0,
          "suffix": " MB",
          "description": "Block size for the probe and copy read loop. Larger chunks reduce overhead but update the progress bar less often."
        },
        {
          "type": "value",
          "attr": "probe_mb",
          "label": "Probe size",
          "kind": "float",
          "decimals": 2,
          "minimum": 0.5,
          "maximum": 256.0,
          "step": 1.0,
          "suffix": " MB",
          "description": "How much of the file head is read to estimate throughput before deciding whether to stage the rest."
        }
      ]
    }
  ]
}
